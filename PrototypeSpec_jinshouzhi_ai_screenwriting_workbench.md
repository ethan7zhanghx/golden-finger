# 金手指 AI 编剧工作台原型规格说明

## 0. 文档信息
- 交付类型：Full prototype package（模块蓝图 + 信息架构 + 核心流程 + 页面规格 + 版本切分）
- 版本：v0.1
- 状态：Working draft
- 更新时间：2026-03-09
- 适用平台：桌面优先 Web 工作台
- 面向对象：甲方 / 领导 / 产品 / 设计 / 前端 / 后端 / AI 算法 / 测试
- 主要参考：`/Users/zhanghaoxin/Desktop/Baidu/【金手指】剧本生成/PRD_jinshouzhi_ai_screenwriting_workbench.md`、`/Users/zhanghaoxin/Desktop/Baidu/【金手指】剧本生成/tmp/pdfs/金手指AI编剧助手.txt`、`/Users/zhanghaoxin/Desktop/Baidu/【金手指】剧本生成/tmp/pdfs/【创意写作】笔花.txt`、`/Users/zhanghaoxin/Desktop/Baidu/【金手指】剧本生成/tmp/pdfs/【笔花】Prompt.txt`
- 当前假设：
  - 本次原型聚焦登录后的产品工作台，不展开官网营销页设计
  - 首发仍以单用户创作为主，不把多人协作和审批流纳入核心原型
  - 原型以 MVP 主闭环为重点，但保留完整产品版的信息架构和扩展位

## 1. 产品定位与设计目标
金手指本期原型不是在设计一个“会聊天的 AI 写作工具”，而是在设计一个围绕剧本项目持续工作的 AI 编剧工作台。核心对象不是一次问答，也不是单篇孤立文档，而是一个包含故事背景、世界观、人物设定、全剧大纲、分集设计、单集剧本、分镜和导出交付的项目容器。用户在这里完成的是连续创作、连续修订和连续沉淀，而不是一次性生成。

结合现有参考内容，金手指应吸收两个方向的优点：一是“金手指 AI 编剧助手”材料里已经明确的 16 步创作方法论、模板库、提示词库和工具矩阵；二是“笔花”所验证的分阶段资产生成、版本回溯和创作模式分流能力。但原型不应简单复制强向导型产品，而要把这些能力转译为“工作台主形态 + 引导式启动 + 上下文化 AI 工具”。这样既保留专业创作者需要的自由度，也保留新手或团队用户需要的方法论抓手。

本次原型设计的成功标准，是让用户在一次连续会话中可以完成以下关键任务：创建或导入一个剧本项目，补齐并确认至少 3 份核心资产，在单集剧本页面持续编辑并调用 AI，最终导出一份可对外评审的 `.docx` 剧本稿。原型需要先把这条最短闭环设计清楚，再承接完整产品版的模板网络、分镜、提示词复用和后续版权入口预留。

- 核心场景：围绕单个短剧项目持续完成策划、写作、修订和导出
- 主要用户：职业编剧、短剧公司策划、工作室编剧成员、内容负责人
- 关键任务：沉淀资产、生成内容、校验一致性、导出交付
- 本次原型目标：冻结工作台骨架、页面结构、关键状态与核心交互规则
- 不覆盖内容：版权交易闭环、复杂多人协作、UGC 模板审核后台、外部数据采集系统

## 2. 整体呈现形式建议
这款产品更适合被设计成“项目制分栏工作台”，而不是 Wizard 式一步一页，也不是纯 Chat 式连续对话。原因在于剧本创作是长链路、高上下文依赖、高自由度的专业工作：用户需要经常在世界观、人物、大纲和单集剧本之间跳转，也需要随时查看 AI 引用了哪些设定、哪些事实已经锁定、哪些下游内容需要复核。只有工作台式结构，才能让信息层级和操作节奏同时稳定。

另一方面，参考产品“笔花”提示我们，完全自由的空白工作区对新手不友好。因此金手指的最佳形态不是“无引导编辑器”，而是“以 IDE 型工作台为主，叠加推荐流程、启动模式和方法论卡片”。用户启动时可以选择自由导入、AI 计划、模板引导三种模式；进入项目后仍能自由跳转，但系统始终给出当前阶段、建议下一步和关键缺失项。这使产品既像一个专业创作空间，又像一个懂流程的编剧搭档。

- 推荐产品形态：项目制 Split-pane Workspace（左导航 + 中编辑 + 右 AI 工具）
- 推荐原因：兼容长文编辑、多对象跳转、上下文引用、AI 预览采纳和版本对比
- 默认导航模式：全局项目工作台 + 项目内文档树 + 右侧上下文化 AI 面板
- 交互节奏：项目启动优先 -> 资产沉淀优先 -> 单集写作优先 -> 格式化与导出收口
- 视觉密度建议：标准偏高密度，接近专业生产工具，而不是轻社交产品
- 信任增强元素：事实锁定标记、上下文来源标签、版本时间线、待复核提示、导出绑定版本号

## 3. 功能模块蓝图
模块划分原则遵循“按用户工作任务组织，而不是按后端服务名组织”。因此原型里的模块应该直接对应用户在一个项目中的真实动作：如何启动、如何看全局、如何沉淀资产、如何写单集、如何调用 AI、如何复用模板、如何导出成果。

| 模块 | 解决的问题 | 核心对象 | 用户关键动作 | 入口 | 角色差异 | 优先级 |
| --- | --- | --- | --- | --- | --- | --- |
| 项目工作台首页 | 让用户快速回到上次工作并管理多个项目 | Project | 新建、继续、筛选、搜索项目 | 登录后默认首页 | 内容负责人更看整体进度，编剧更看最近编辑 | P0 |
| 项目启动与导入 | 让用户不必从空白页开始 | ProjectMeta、ImportTask | 选择启动模式、填写基础信息、导入资料 | 新建项目、模板卡、导入按钮 | 新手偏引导模式，熟练用户偏自由导入 | P0 |
| 项目概览与阶段引导 | 让用户知道项目当前进展和下一步 | Project、StageChecklist | 查看进度、补齐缺失资产、跳转推荐页面 | 项目默认页 | 内容负责人更关注完成度 | P0 |
| 项目内工作台壳层 | 让用户在跨文档写作时始终保持稳定上下文 | ProjectShell、EditorTab | 导航切换、多标签编辑、收起侧栏、全局搜索 | 打开任意项目后 | 差异不大 | P0 |
| 创作资产系统 | 形成可引用、可确认、可锁定的上游设定 | AssetDoc、LockedFact | 编辑、生成、确认、锁定、复核 | 左侧资产树、推荐下一步 | 编剧更高频使用 | P0 |
| 创作产出系统 | 承接具体交付内容而非停留在设定层 | OutputDoc | 创建单集、编辑、生成分镜、格式化 | 左侧产出树、概览页 CTA | 编剧与策划都会使用 | P0 |
| 主编辑器与版本对比 | 提供持续写作和回溯能力 | EditorTab、Version | 多标签编辑、保存、对比、恢复 | 中间主工作区 | 差异不大 | P0 |
| AI 工具与采纳面板 | 把 AI 调用变成可控的建议系统 | AIRun、Suggestion | 运行工具、调整上下文、采纳、放弃、转版本 | 右侧工具栏 | 差异不大 | P0 |
| 模板库与提示词库 | 支持跨项目复用和提效 | Template、PromptRecipe | 浏览、预览、应用、复制、收藏 | 全局导航、项目资源区 | 团队负责人更关注团队模板 | P1 |
| 格式化与导出 | 把工作成果变成可外发文件 | ExportTask | 格式化、预览、导出、选择版本 | 顶部栏、剧本文档页 | 内容负责人导出频率更高 | P0 |
| 分镜扩展页 | 把剧本转为后续制作材料 | StoryboardDoc | 从剧本生成、人工编辑、导出表格 | 单集剧本页、产出树 | 偏策划与制作协同 | P1 |

## 4. 信息架构
信息架构需要同时解决两个问题：一是让用户从产品层面看懂“我在管理哪些项目”，二是让用户进入项目后清楚“这个项目里有哪些资产、有哪些产出、当前走到哪一步”。因此原型采用两层结构：全局层负责项目与资源入口，项目层负责真实创作工作。

### 4.1 导航结构
- 全局一级导航：项目工作台、模板库、提示词库
- 登录后默认首页：项目工作台首页
- 项目内工作台壳层：顶部栏 + 左文档树 + 中央多标签工作区 + 右 AI 控制台，作为所有项目内任务页共享框架
- 项目内顶部栏：项目名称、项目基础信息、阶段状态、自动保存状态、导入、导出、全局搜索
- 项目内左侧导航：
  - 项目概览
  - 推荐流程（方法论展开页）
  - 创作资产
    - 故事背景
    - 世界观设定
    - 核心卖点
    - 爽点和钩子
    - 主要角色
    - 次要角色
    - 人物关系
    - 人物弧光
    - 全剧大纲
    - 分集设计
  - 创作产出
    - 单集剧本
    - 分镜
    - 剧本格式稿
    - 剧本介绍
    - 剧名方案
  - 参考与资源
    - 导入资料（项目级资料管理页）
    - 模板库
    - 提示词库
  - 版本记录
- 项目内右侧面板：推荐工具、上下文来源、生成结果、差异对比、冲突提示、采纳操作

### 4.2 对象层级
- Workspace -> Project -> ProjectMeta
- Project -> AssetDoc -> LockedFact -> Version
- Project -> OutputDoc -> Suggestion -> Version
- Project -> ReferenceDoc / ImportSnapshot
- Project -> AIRun -> Suggestion

建议在原型中把对象层级隐性体现在页面结构里，而不是直接暴露技术名词。例如用户看到的是“世界观文档已锁定”“第 3 集剧本待复核”“本次 AI 建议来自世界观 + 人物设定 + 第 2 集剧本”，而不是看到底层对象模型。

### 4.3 页面层级示意
```text
项目工作台首页
├── 新建项目
├── 最近项目
└── 快捷入口（模板库 / 提示词库）

项目工作台 / 某个项目
├── 项目概览
├── 推荐流程
├── 创作资产
│   ├── 世界观设定
│   ├── 角色与关系
│   ├── 全剧大纲
│   └── 分集设计
├── 创作产出
│   ├── 第 N 集剧本
│   ├── 第 N 集分镜
│   └── 剧本格式稿
├── 参考与资源
│   ├── 导入资料
│   ├── 模板库
│   └── 提示词库
└── 版本记录
```

### 4.4 入口与回路设计
- 从项目工作台首页可以进入最近项目，也可以通过“新建项目”进入启动流程
- 从新建项目页可以直接导入资料、使用模板，或进入 AI 计划模式
- 从项目概览页可以跳转到“推荐下一步”文档，也可以直接进入任意资产或单集剧本
- 从推荐流程页可以按阶段进入目标资产/产出页，也允许用户跳过推荐顺序直接编辑
- 从顶部栏“导入”或左侧“导入资料”可以进入资料管理页，查看解析结果并映射到目标文档
- 从任何文档页都能通过右侧工具栏调用 AI，并把结果回写当前文档、回写为新资产文档或存为新版本
- 从导出完成页可以返回当前剧本、打开格式稿，或继续创建下一集

## 5. 核心流程
流程设计的关键，不是把所有步骤串成一条死板链路，而是把“推荐路径”和“自由路径”同时设计清楚。主流程服务于首发闭环，首次流程服务于新手理解产品，自由路径服务于专业编剧，异常流程服务于 AI 产品最容易出问题的环节。

### 5.1 主流程（MVP 闭环）
1. 用户进入项目工作台首页，点击“新建项目”。
2. 用户选择启动模式：`自由导入`、`AI 计划`、`模板引导`。
3. 用户填写项目基础信息，系统创建默认文档树，并生成项目概览页。
4. 用户在概览页看到推荐下一步，先补齐世界观、人物设定、全剧大纲等核心资产。
5. 用户在资产页调用 AI 工具生成或修订内容，并对关键信息执行“确认/锁定”。
6. 用户进入某一集剧本页，在主编辑器中持续写作，右侧按上下文推荐“剧情和桥段”“开头写作”“台词写作”等工具。
7. AI 结果先进入待采纳态，用户选择插入、替换、追加为版本或放弃。
8. 用户点击“剧本格式化”，生成格式稿，并在导出弹窗里导出 `.docx`。
9. 系统记录导出版本、导出时间和关联项目元信息，形成可追溯交付物。

### 5.2 首次使用流程
1. 首次进入产品时，首页优先显示“新建项目”和“从模板开始”两个强入口。
2. 如果用户选择 `AI 计划` 模式，系统用 3-5 个问题帮助其收束题材、核心冲突、目标集数、受众方向。
3. 系统根据回答生成一份可编辑的项目描述，并推荐适配模板或默认文档骨架。
4. 用户进入项目概览页后，系统用“阶段进度条 + 缺失项提示 + 推荐下一步”说明当前应先补齐什么。
5. 在首个空白资产页里，空态明确给出“手动写作”和“AI 生成初稿”两个入口，避免用户被空白页卡住。

### 5.3 熟练用户自由路径
1. 用户可跳过引导，直接导入已有大纲、人物设定或单集剧本。
2. 系统要求用户在导入时选择目标文档，并自动创建缺失节点。
3. 如果用户直接进入单集剧本页，系统通过黄色提示条标识“建议补齐世界观 / 人物 / 大纲后获得更稳定结果”，但不强制阻断写作。
4. 用户可直接选中文本调用 AI 改写、续写、优化台词，而无需先完成全部上游资产。

### 5.4 异常与回退流程
- AI 生成失败：保留当前文本和参数，支持一键重试或切换工具
- 上下文不足：提示“将按当前已有信息生成，结果可能不稳定”，用户可继续
- 锁定事实冲突：结果进入待采纳态并显示冲突项，不直接写回正文
- 上游资产变更：相关下游文档标记为 `待复核`，但正文不自动重写
- 导出前发现待复核：导出弹窗提示风险，允许返回复核后再导出
- 用户误恢复历史版本：恢复动作本身生成新版本，不覆盖原时间线

### 5.5 空态与不可用路径
- 空项目：直接显示推荐下一步、导入资料入口和官方模板卡片
- 空文档：显示“手动开始”和“AI 生成初稿”双 CTA
- 空单集剧本：支持从分集设计生成一版骨架，也支持从零开始
- 空分镜页：提示“请先完成对应单集剧本”
- 团队模板无权限：显示不可编辑状态和来源说明，但不把整个页面做成报错态

## 6. 页面 / 屏幕规格
页面规格以“任务页优先”组织：先定义全局入口和项目壳层，再定义最核心的创作页面，最后补足资源页和导出页。每个页面都需要覆盖目标、状态和边界，避免原型只停留在组件排布层。

### Screen 0. 项目工作台壳层（项目内全局布局）
- 页面目标：为项目内所有任务页提供稳定的导航、编辑和 AI 协作骨架，确保用户在长链路创作中不丢失上下文
- 入口：从项目工作台首页打开任意项目后进入；项目内全部页面共享该壳层
- 目标用户：全部创作用户
- 核心组件：
  - 顶部栏：项目名称、阶段状态、自动保存状态、导入、导出、全局搜索、版本入口
  - 左侧导航树：项目概览、推荐流程、创作资产、创作产出、参考与资源、版本记录
  - 中央工作区：支持多标签打开页面和文档，保留最近访问顺序
  - 右侧 AI 控制台：可固定 / 收起，跟随当前激活标签切换上下文
  - 壳层控制区：左右栏展开收起、宽度拖拽、风险标记与未读提示
- 主操作：切换导航、打开文档、多标签切换、收起侧栏、全局导入导出
- 次操作：全局搜索、回到最近编辑、查看项目状态、打开版本记录
- 字段与校验：
  - 切换标签或页面前需保留未保存草稿，不允许因切换造成正文丢失
  - 导入、导出等全局操作必须明确当前作用项目，避免跨项目误操作
- 业务规则：
  - 左侧树和右侧 AI 面板为项目级常驻区，不随单页切换而重建
  - 中央区允许同时打开多个文档，但同一文档重复打开时应回到已存在标签
  - 当当前标签存在 `needs_review` 或冲突时，壳层级展示风险标记，避免用户切页后忽略风险
  - 左右侧栏即使收起，也要保留最小可点击入口和状态提示
- 成功态：用户可在不离开项目上下文的情况下完成跨文档跳转、持续写作与 AI 调用
- 空态：项目首次创建后，中央区默认落到项目概览页或推荐流程页
- 加载态：先加载壳层骨架，再分块加载当前标签内容与 AI 上下文
- 错误态：局部页面失败不影响壳层存在，至少保留导航、最近标签和刷新入口
- 埋点建议：`project_shell_view`、`project_nav_click`、`project_tab_switch`、`project_panel_toggle`
- 备注：这是工作台体验的一号 contract，应先于单页视觉细节冻结

### Screen 1. 项目工作台首页
- 页面目标：让用户快速找到要继续的项目，并从这里进入新建、模板启动或导入流程
- 入口：登录后默认进入；顶部导航“项目工作台”
- 目标用户：全部创作用户
- 核心组件：
  - 顶部欢迎区 + “新建项目”主按钮
  - 最近编辑项目列表
  - 项目筛选区（题材、状态、最近更新时间）
  - “继续上次创作”快捷卡
  - 官方模板快捷卡、提示词库快捷入口
- 主操作：打开项目、创建项目、从模板开始、导入已有资料
- 次操作：搜索项目、筛选项目、查看最近导出记录
- 字段与校验：搜索词可模糊匹配项目名和标签；筛选条件支持多选
- 业务规则：
  - 未完成项目优先展示“推荐下一步”
  - 最近编辑文档应直接可见，例如“第 3 集剧本”
- 成功态：用户一键进入指定项目或启动新项目
- 空态：展示“还没有项目”的引导文案、模板卡和导入入口
- 加载态：骨架屏先展示项目卡片轮廓
- 错误态：项目列表加载失败时给出刷新按钮，不影响新建项目入口
- 埋点建议：`workspace_home_view`、`project_open_click`、`project_create_click`
- 备注：该页承担“产品首页”角色，不承载复杂深度编辑功能

### Screen 2. 新建项目 / 启动模式页
- 页面目标：把一个创意、已有资料或模板快速转化为项目骨架
- 入口：项目工作台首页“新建项目”；模板卡“使用模板”
- 目标用户：首次使用者、从零启动用户、已有资料导入用户
- 核心组件：
  - 启动模式选择卡：`自由导入`、`AI 计划`、`模板引导`
  - 基础信息表单：项目名称、题材、风格、目标集数、单集时长、创作描述
  - 参考资料上传区：`.docx`、`.txt`、粘贴文本
  - 模板预览区或 AI 问答区（随模式切换）
  - 默认生成文档树预览
- 主操作：创建项目、导入资料、选择模板、进入 AI 问答
- 次操作：保存草稿、返回首页
- 字段与校验：
  - 项目名称必填
  - 集数、时长为结构化字段，支持后续编辑
  - 导入时必须选择目标文档类型
- 业务规则：
  - `自由导入` 适合已有大纲/剧本的熟练用户
  - `AI 计划` 通过问答生成项目描述，适合概念还不完整的用户
  - `模板引导` 通过题材模板预填字段，适合快速启动
  - 创建成功后必须生成默认资产树和产出树
- 成功态：跳转至项目概览页，并显示创建成功提示
- 空态：模式未选择时展示三张模式说明卡片
- 加载态：AI 计划模式生成项目描述时展示阶段文案
- 错误态：导入解析失败时保留文件记录和表单内容，允许重试或改为粘贴文本
- 埋点建议：`project_start_mode_select`、`project_create_success`、`project_import_success`
- 备注：这里是对“笔花”三种创作模式的吸收，但输出目标改为项目骨架，而不是把用户锁进向导流程

### Screen 3. 项目概览页
- 页面目标：让用户理解项目当前状态、缺失项和推荐下一步
- 入口：项目创建完成后的默认落点；左侧导航“项目概览”
- 目标用户：全部创作用户，尤其是首次进入某项目时
- 核心组件：
  - 项目头部信息卡（题材、风格、目标集数、单集时长、项目状态）
  - 四阶段进度条：准备、策划、创作、完善
  - 核心资产完成度卡片
  - 最近编辑与最近 AI 记录
  - 推荐下一步卡片
  - 风险提醒区（待复核、冲突、未导出）
- 主操作：跳转推荐文档、快速创建新资产、创建第 N 集剧本、导入资料
- 次操作：编辑项目基础信息、查看版本记录、进入模板库或提示词库
- 字段与校验：阶段完成度基于文档存在、有内容、已确认三层规则计算
- 业务规则：
  - 推荐下一步优先指向阻塞主闭环的缺失资产
  - 如已有 3 份核心资产，则概览页主 CTA 优先切到“进入单集剧本”
- 成功态：用户明确知道下一步该做什么，并能一键进入
- 空态：只显示项目基础信息和“从模板 / 从导入 / 从世界观开始”三条推荐路径
- 加载态：概览指标、最近编辑和进度卡分块加载
- 错误态：部分卡片失败不影响页面整体使用，失败卡片支持单独刷新
- 埋点建议：`project_overview_view`、`next_step_click`、`stage_progress_view`
- 备注：该页是方法论承接位，不是数据仪表盘式管理后台

### Screen 3A. 推荐流程页
- 页面目标：把“16 步方法论”转成可执行、可跳过、可追踪的阶段化创作路径，而不是把用户锁死在向导里
- 入口：左侧导航“推荐流程”；项目概览页“查看完整流程”；新建项目完成后的首次引导
- 目标用户：首次使用者、新手编剧、希望按方法推进的策划用户
- 核心组件：
  - 阶段泳道：准备、策划、创作、完善四阶段
  - 步骤卡片：步骤说明、推荐产出、完成标准、预计耗时
  - 当前建议区：系统判断的“现在最值得做的一步”
  - 依赖提示区：当前步骤依赖哪些已确认资产
  - 快捷入口：跳转目标文档、调用推荐 AI 工具、应用模板
- 主操作：查看阶段步骤、进入推荐文档、标记已完成、跳过当前建议
- 次操作：展开步骤说明、查看示例、切换到自由编辑、回到项目概览
- 字段与校验：
  - 步骤完成判断需基于实际文档状态，不允许只点按钮就伪完成
  - 用户跳过步骤时需记录原因或至少保留“已跳过”标记
- 业务规则：
  - 推荐流程是方法论展开页，不等于强制向导，用户可随时跳出到任意文档
  - 同一时刻只高亮一个“推荐下一步”，避免并行推荐造成决策噪音
  - P1/P2 扩展步骤可展示占位，但默认折叠，避免干扰 V1 主闭环
- 成功态：用户明确知道当前阶段、推荐动作和完成标准，并一键进入目标页面
- 空态：新项目尚无任何资产时，优先显示“从世界观 / 从模板 / 从导入开始”三条路径
- 加载态：先加载阶段骨架，再逐步计算每步完成度
- 错误态：流程完成度计算失败时仍展示固定方法论顺序，不阻断进入文档
- 埋点建议：`recommended_flow_view`、`recommended_step_click`、`recommended_step_skip`
- 备注：该页和项目概览页的关系应明确为“概览负责摘要，推荐流程负责展开”

### Screen 3B. 导入资料页
- 页面目标：集中管理项目内外部资料的导入、解析、映射和沉淀，避免导入动作只停留在一次性弹窗里
- 入口：顶部栏“导入”；左侧导航“导入资料”；新建项目页导入成功或失败后的继续处理
- 目标用户：熟练编剧、策划、已有历史资料的迁移用户
- 核心组件：
  - 上传区：支持 `.docx`、`.txt`、粘贴文本
  - 导入任务列表：文件名、来源、解析状态、导入时间、目标文档
  - 内容预览区：原文快照、抽取摘要、识别到的角色/设定/集数信息
  - 映射配置区：选择写入目标资产或保留为参考资料
  - 冲突与异常区：格式异常、识别失败、与现有锁定事实冲突提示
- 主操作：上传资料、预览解析结果、映射到目标文档、保存为参考资料、重试解析
- 次操作：删除未生效任务、复制摘要、跳转到受影响文档、下载原文件
- 字段与校验：
  - 导入时必须指定“保存为参考资料”或“映射到目标文档”之一
  - 对映射到正式文档的导入，需明确覆盖、追加还是生成新版本
- 业务规则：
  - 导入原文件和解析快照需可追溯保存，便于后续回看来源
  - 解析失败时不可丢失原文件记录，用户可改目标文档或改用手动粘贴
  - 与锁定事实冲突的导入内容不可静默写入正式文档，只能进入待采纳或待处理状态
- 成功态：资料被保存为参考资料，或按指定方式写入目标文档并生成版本记录
- 空态：展示支持格式、推荐导入顺序和“没有资料也可从模板开始”的说明
- 加载态：按文件维度展示上传、解析、映射三个阶段进度
- 错误态：单个文件解析失败不影响其他文件继续导入，失败项支持单独重试
- 埋点建议：`import_center_view`、`import_upload_start`、`import_parse_success`、`import_apply_click`
- 备注：该页应承接“启动时导入”和“项目中途补资料”两类场景，避免重复设计两套入口

### Screen 4. 资产文档页（通用：世界观 / 卖点 / 人物 / 大纲 / 分集设计）
- 页面目标：让用户沉淀、编辑并确认可供下游引用的核心资产
- 入口：左侧资产树；项目概览页推荐下一步；AI 结果“保存为资产”
- 目标用户：编剧、策划、内容负责人
- 核心组件：
  - 页面头部：文档名称、状态、最近更新时间、确认/锁定按钮
  - 主编辑器：支持长文编辑与结构化锚点导航
  - 结构化摘要区：提取关键角色、设定、冲突、时间线
  - 文档引用提示：显示本页被哪些下游文档引用
  - 右侧 AI 工具推荐区：世界观设定、核心卖点、骨骼框架、人物写作等
- 主操作：手动编辑、AI 生成初稿、AI 修订、确认、锁定、创建新版本
- 次操作：查看引用关系、复制链接、导入内容、恢复历史版本
- 字段与校验：
  - 确认前需至少存在正文内容
  - 锁定前需要用户二次确认“将作为下游高优先级事实”
- 业务规则：
  - 已锁定资产仍可编辑，但编辑前必须提示会影响下游文档
  - AI 输出如与锁定事实冲突，只能作为建议展示
  - 人物名、关系、关键设定建议结构化抽取，便于后续引用
- 成功态：文档更新并进入 `draft` / `confirmed` / `locked` 对应状态
- 空态：展示“手动写作”和“AI 生成初稿”双入口，并说明推荐上下文来源
- 加载态：文档正文与结构化摘要分开加载
- 错误态：保存失败时保留本地草稿并提示重试；结构化抽取失败不阻断正文编辑
- 埋点建议：`asset_doc_view`、`asset_confirm_click`、`asset_lock_click`、`asset_ai_run`
- 备注：页面框架统一，但不同资产类型可替换顶部结构化模块

### Screen 5. 单集剧本编辑页
- 页面目标：承载真实创作主战场，支持持续编辑、局部改写和格式化前收口
- 入口：左侧产出树“第 N 集剧本”；概览页“进入创作”；从分集设计创建
- 目标用户：编剧、策划
- 核心组件：
  - 集数头部信息：第几集、状态、最近保存时间、待复核提示
  - 主编辑器：剧本文本编辑区、段落锚点、选中文本工具条
  - 上游摘要条：当前引用的大纲、人物、世界观摘要
  - AI 结果停靠区：最近一次建议、差异预览入口
  - 下游动作条：生成分镜、剧本格式化、导出
- 主操作：手动写作、续写、改写选中内容、生成开篇、优化台词、格式化、导出
- 次操作：查看上游摘要、切换其他标签、创建新版本、跳转相关资产
- 字段与校验：
  - 新建单集时需选择集数
  - 选中文本改写默认作用于选区；未选中时作用于当前段落或全文
- 业务规则：
  - 如果缺少关键资产，允许继续写，但在页头提示质量风险
  - 如上游锁定事实已变更，页面进入 `待复核` 状态并突出冲突来源
  - 分镜生成按钮在正文为空时置灰并提示原因
  - “格式化完成”“可提交评审”属于辅助标签，不单列为生命周期状态
- 成功态：剧本可处于 `empty` / `draft` / `confirmed` / `needs_review`；若当前版本已被导出，则在页头和版本时间线追加 `exported` 标记
- 空态：支持“从分集设计生成骨架”或“从空白开始写”两条路径
- 加载态：长文加载时先展示文档骨架和最近版本信息
- 错误态：AI 结果失败不打断正文编辑；自动保存失败时给出本地恢复提示
- 埋点建议：`episode_script_view`、`episode_script_ai_run`、`script_format_click`、`export_click`
- 备注：这是整个产品最核心的 P0 页面，原型应优先细化交互细节

### Screen 6. AI 工具面板与建议采纳侧栏
- 页面目标：把 AI 调用变成可解释、可调整、可采纳的建议系统，而不是黑盒结果框
- 入口：任意资产页或剧本页右侧固定栏
- 目标用户：全部创作用户
- 核心组件：
  - 工具推荐列表：根据当前文档类型动态排序
  - 上下文来源区：项目基础信息、已确认资产、锁定事实、当前文档、选中内容
  - 参数区：输出风格、长度、目标对象、保守/发散程度
  - 运行状态区：排队、生成中、耗时、取消按钮
  - 结果区：正文预览、差异视图、冲突标签、来源解释
  - 采纳操作区：插入、替换、追加为版本、复制、放弃、保存为资产
- 主操作：运行工具、切换上下文、采纳结果、保存为版本、放弃
- 次操作：重试、复制结果、查看冲突详情、反馈结果质量
- 字段与校验：
  - 对依赖前置资产的工具，缺失时给出可继续但有风险的提示
  - 若用户已选中正文，默认目标范围锁定到选区
- 业务规则：
  - 所有 AI 结果先进入待采纳态，不允许无感覆盖正文
  - 冲突结果必须附带具体冲突项和来源，而不是笼统报错
  - 追加为版本时需记录工具名、触发时间和上下文快照摘要
- 成功态：结果被采纳并写入文档，或保存为新版本/新资产
- 空态：未选中文档时显示“打开文档后可获得推荐工具”
- 加载态：展示工具名称、目标对象、上下文来源和阶段式文案
- 错误态：保留用户参数和最后一次上下文配置，支持重试
- 埋点建议：`ai_tool_open`、`ai_run_start`、`ai_suggestion_adopt`、`ai_suggestion_reject`
- 备注：该侧栏本质上是产品的“AI 控制台”，在信息密度上可比普通抽屉更专业

### Screen 7. 模板库页
- 页面目标：支持用户快速启动项目，并把创作经验沉淀为可复用资产
- 入口：全局导航“模板库”；项目内左侧“模板库”；新建项目页模板模式
- 目标用户：全部创作用户，尤其是团队与公司用户
- 核心组件：
  - 模板分层标签：官方模板、团队模板、UGC 模板（后续版本）
  - 筛选器：题材、风格、集数区间、适用阶段、更新时间
  - 模板卡片：简介、标签、来源、适用场景、最近使用次数
  - 模板预览抽屉：预填字段、包含文档、将覆盖的内容、授权说明
- 主操作：预览模板、应用到新项目、应用到当前项目、收藏
- 次操作：复制为私有模板、查看来源、按团队筛选
- 字段与校验：应用模板前必须明确作用范围和将被预填的字段
- 业务规则：
  - MVP 只开放官方模板和团队模板，UGC 模板预留入口但不开放发布
  - 模板是复用资产，不可直接替代项目事实；应用后内容默认进入 `draft`
- 成功态：模板应用成功并跳转到项目概览或目标文档
- 空态：若团队暂无模板，优先展示官方模板和“沉淀团队模板”的说明
- 加载态：模板列表骨架屏 + 预览抽屉延迟加载
- 错误态：模板详情加载失败不影响列表浏览
- 埋点建议：`template_library_view`、`template_preview_open`、`template_apply_click`
- 备注：这里承接“金手指”参考材料中的模板库价值，也承接 PRD 的模板资产网络定义

### Screen 8. 提示词库页
- 页面目标：让 Prompt 作为复用资产存在，但不绕开项目资产体系
- 入口：全局导航“提示词库”；项目内资源区；AI 工具面板“更多 Prompt”
- 目标用户：熟练创作者、策划、团队模板维护者
- 核心组件：
  - 任务分类：故事梗概、人物小传、分集设计、单集剧本、台词优化、剧名生成、剧本介绍
  - Prompt 卡片：任务说明、适用前提、输出对象、示例变量
  - 预览区：完整 Prompt、变量说明、使用注意事项
  - 应用区：选择目标项目和目标文档
- 主操作：查看 Prompt、复制 Prompt、应用到当前文档、收藏常用 Prompt
- 次操作：按任务筛选、查看最近使用、对 Prompt 打标签
- 字段与校验：
  - 应用 Prompt 时需指定目标文档；若没有打开项目，则只支持复制
  - 对需要前置资产的 Prompt，需展示缺失项提示
- 业务规则：
  - Prompt 是“方法资产”，不应替代项目文档的确认/锁定机制
  - 与当前项目关联使用时，优先走 AI 工具面板统一执行链路
- 成功态：Prompt 被复制或作为工具预设载入 AI 面板
- 空态：默认显示推荐任务类 Prompt 和使用说明
- 加载态：分类和卡片列表可分块加载
- 错误态：Prompt 详情失败时不影响卡片浏览
- 埋点建议：`prompt_library_view`、`prompt_copy_click`、`prompt_apply_click`
- 备注：该页参考“金手指”已有提示词库栏目，但产品内要强化“应用到当前项目”的闭环

### Screen 9. 版本记录与差异对比页
- 页面目标：让用户能回溯每次导入、人工编辑和 AI 采纳带来的变化
- 入口：文档头部“版本记录”；项目左侧“版本记录”；导出成功后的版本链接
- 目标用户：全部创作用户，尤其是内容负责人和反复修稿用户
- 核心组件：
  - 时间线列表：版本时间、操作人、变更来源、工具名
  - 差异对比区：旧版 / 新版对照或 inline diff
  - 版本标签：手动保存、AI 追加、导入生成、导出版本
  - 恢复与另存为按钮
- 主操作：查看版本差异、恢复历史版本、另存为新版本
- 次操作：筛选 AI 版本、筛选导出版本、复制版本摘要
- 字段与校验：恢复版本前提示“恢复将生成新版本，不覆盖旧版本”
- 业务规则：
  - 导出过的版本需有明显标记
  - 恢复到旧版本后，如果上游事实已变化，应打上 `待复核` 标签
- 成功态：新版本生成并回到原文档继续编辑
- 空态：新文档尚无历史版本时展示首次保存说明
- 加载态：先展示时间线，再加载差异详情
- 错误态：差异加载失败不影响时间线浏览
- 埋点建议：`version_history_view`、`version_compare_open`、`version_restore_click`
- 备注：该页需要和 AI 建议面板、导出页共同形成完整追溯链路

### Screen 10. 剧本格式化与导出弹窗
- 页面目标：把当前剧本或格式稿整理为可外发的交付文件
- 入口：顶部栏“导出”；单集剧本页“剧本格式化 / 导出”
- 目标用户：编剧、策划、内容负责人
- 核心组件：
  - 导出对象选择：单集剧本、剧本格式稿、后续扩展对象
  - 导出版本选择：当前版本、指定历史版本
  - 导出预检查：空内容、待复核提醒、格式化是否完成
  - 文件信息区：文件名、版本号、导出时间、项目名、集数
  - 导出格式区：MVP 仅 `.docx`
- 主操作：先格式化、预览、导出
- 次操作：返回文档继续修改、查看待复核项、复制导出摘要
- 字段与校验：
  - 空内容不可导出正式成品，但可导出空模板
  - 待复核文档允许导出，但需二次确认
- 业务规则：
  - 导出必须绑定明确版本
  - 格式化应保留原义，不自动改写剧情
  - 导出完成后记录在版本时间线中
- 成功态：导出成功，展示文件名和版本摘要
- 空态：无可导出对象时提示先完成单集剧本
- 加载态：格式化和导出分别展示独立进度
- 错误态：格式化失败时保留原文并支持重试；导出失败时不丢失导出配置
- 埋点建议：`export_modal_open`、`format_run_start`、`export_success`
- 备注：这是从“工作台内部创作”切到“对外交付”的关键关口，信任提示要比普通下载弹窗更强

### Screen 11. 分镜页（P1 扩展）
- 页面目标：把单集剧本转化为制作可用的分镜草稿，并允许人工继续编辑
- 入口：单集剧本页“生成分镜”；左侧产出树“第 N 集分镜”
- 目标用户：策划、编剧、后续制作协同角色
- 核心组件：
  - 对应剧本信息条
  - 分镜表格区：场次、镜号、景别、动作、对白、时长、备注
  - AI 生成入口与再整理入口
  - 与原剧本联动查看区
- 主操作：从当前剧本生成分镜、编辑分镜、导出表格
- 次操作：回看原剧本、按场次折叠、重生成局部场景
- 字段与校验：无剧本文本时不可直接生成完整分镜
- 业务规则：
  - 分镜生成必须绑定来源剧本版本
  - 剧本文本更新后，分镜页进入 `待复核`
- 成功态：分镜表生成并可继续人工编辑
- 空态：提示“先完成单集剧本再生成分镜”
- 加载态：按场次分段生成并显示进度
- 错误态：局部场次生成失败时支持单段重试
- 埋点建议：`storyboard_view`、`storyboard_generate_click`、`storyboard_export_click`
- 备注：该页不作为 MVP 强依赖，但建议在原型中预留位置，避免后续结构返工

## 7. 关键交互规则
关键交互规则的目标，是让设计、研发和 AI 方案在同一套产品语义下协作。这里优先冻结那些一旦改动就会影响整套体验的规则，而不是把所有视觉细节都在此阶段锁死。

### 7.1 文档状态与提醒语义
- `empty`：已创建但无正文内容
- `draft`：已有内容但尚未确认
- `confirmed`：已人工确认，可作为稳定事实来源
- `locked`：高优先级事实，不允许被 AI 静默推翻
- `needs_review`：上游事实变更后，需要人工复核
- `exported`：该版本已被对外导出

补充说明：
- `locked` 主要用于世界观、人物、关系等高优先级资产，通常不用于普通剧本正文
- 单集剧本进入导出前，以 `confirmed` 作为默认可导出版本；“格式化完成”“待提交评审”属于辅助标签，不单列为生命周期状态
- 项目阶段、推荐下一步、格式化完成等都属于页面提示或流程标签，不与文档状态混用

建议在导航树、文档头部、导出弹窗中统一使用同一套状态标签，避免一处叫“待复核”、一处叫“待检查”、一处叫“风险中”的命名混乱。

### 7.2 搜索、筛选与导航
- 左侧文档树支持按名称和类型搜索
- 单集剧本和分镜按集数分组展示，避免列表过长
- 模板库和提示词库都应支持题材、风格、任务类型筛选
- 当前文档在树中需要高亮并自动定位

### 7.3 保存、草稿与恢复
- 主编辑器默认自动保存，并在顶部展示最近保存时间
- 文档切换时必须保留未提交草稿，不要求用户手工确认每次跳转
- 网络异常时优先保留本地缓存，并在恢复后提示是否合并
- 恢复历史版本不覆盖旧版本，而是新建一个恢复版本

### 7.4 AI 建议采纳与差异预览
- AI 输出全部进入 `待采纳` 状态
- 默认支持三类高频采纳动作：插入、替换、追加为版本
- 长文本建议优先提供差异视图，而不是整段纯文本堆叠
- 冲突标签需给到具体项，例如“人物名冲突：林国栋 / 林国梁”
- 保存为资产时必须要求用户指定目标资产类型

### 7.5 复核传播规则
- 当世界观、人物、关系、集数规格等高优先级信息变化时，依赖的下游文档进入 `待复核`
- `待复核` 不等于禁止继续编辑，而是强调“当前内容可能已与最新设定不一致”
- 导出前如果存在待复核项，需在弹窗中明确提示来源和影响范围

### 7.6 权限与可见性
- MVP 默认不设计复杂协作权限，只保留 owner 语义
- 团队模板可见但不可编辑时，用禁用态 + 来源说明，而不是直接报错
- UGC 模板发布、审核、署名管理在完整产品版处理，不占用首版工作台主流程

## 8. V1 切片建议
版本切片不应把完整方案拆得支离破碎，而应保证第一版已经能完整试用。因此切片建议按照“是否支撑一集剧本闭环”来判断，而不是按功能名字轻重粗暴裁掉。

### 8.1 V1 必做
- 页面：项目内工作台壳层、项目工作台首页、新建项目页、项目概览页、推荐流程页、导入资料页、资产文档页、单集剧本页、AI 工具面板、模板库页、版本记录页、格式化与导出弹窗
- 模块：项目创建与导入、项目内工作台壳层、默认文档树、核心资产管理、单集剧本编辑、AI 建议采纳、`.docx` 导出
- 工具：世界观设定、核心卖点、骨骼框架、人物写作、单集剧本助手、剧本格式化
- 资源：官方模板、团队模板、基础提示词库浏览与应用
- 状态：确认 / 锁定 / 待复核 / 导出版本追溯

### 8.2 P1 可延后但应在原型中预留
- 分镜页及其导出能力
- 提示词库更深的变量化编辑
- 项目内更完整的阶段看板
- 角色与关系的结构化图谱视图
- 更细的节奏分析与一致性检查面板

### 8.3 后续版本再进入
- 扫榜调研自动化
- 编剧素质训练与方法论内容中心
- UGC 模板发布、审核、推荐和授权
- 剧名创作、剧本介绍等完善阶段成套页面
- 多人协作、审批流、提审流
- 版权能力的产品化入口

## 9. 开放问题与交接说明
### 9.1 进入设计前建议先确认的问题
- 项目工作台首页是否需要兼做“登录后首页”，还是另有产品首页
- `AI 计划` 模式的问题数量和题目维度是否需要先冻结一版
- 模板应用时，哪些字段允许预填，哪些字段必须由用户二次确认
- 分镜导出的行业模板是否已有指定格式
- 待复核提示是否需要区分“强提醒”和“弱提醒”两级
- 是否要在项目概览页保留未来版权能力占位入口

### 9.2 设计风险
- 如果把项目概览做成纯数据看板，会弱化方法论引导价值
- 如果把 AI 工具面板做成普通聊天抽屉，会削弱“上下文化工具”的专业感
- 如果资产页和剧本页结构差异过大，会破坏工作台一致性
- 如果状态标签命名不统一，用户会难以理解“确认、锁定、待复核”的差别

### 9.3 技术风险
- 长文本编辑与自动保存性能需要优先验证
- AI 上下文拼装与冲突检测会直接影响建议质量和可控性
- 版本对比、导出追溯和待复核传播需要统一数据模型支撑
- 导入 `.docx` 与导出 `.docx` 的保真度会显著影响甲方感知

### 9.4 给 Figma / 研发的交接口径
- 先冻结项目内工作台壳层，再画 P0 的 8 个关键任务页：项目工作台首页、新建项目页、项目概览页、推荐流程页、导入资料页、资产文档页、单集剧本页、AI 工具面板
- 在第二轮补齐模板库、版本记录页、导出弹窗和分镜扩展页
- 设计稿中务必体现 4 组关键状态：空态、生成中、冲突提醒、待复核
- 研发方案需与 PRD 附录中的 `Project / AssetDoc / OutputDoc / LockedFact / AIRun / Suggestion / Version` 语义对齐
