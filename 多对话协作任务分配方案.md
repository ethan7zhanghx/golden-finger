# 金手指项目多对话协作任务分配方案（Codex-only）

## 1. 协作原则
- 你开多个对话，每个对话只负责一个工作流，避免互相覆盖。
- 主对话（本对话）只做：总控排期、接口冻结、集成验收、上线决策。
- 每个子对话必须有固定产出文件路径，避免口头结论丢失。

## 2. 建议开 5 个对话（含主对话）

### 对话 A（主对话）：总控与集成
- 职责：日计划、跨组依赖、版本冻结、上线 Go/No-Go。
- 固定产出：
  - `MVP_三周冲刺详细计划书_20260318_20260407.md`
  - `integration/集成验收清单.md`（后续创建）

### 对话 B：AI Tool Contract & Prompt Contract
- 职责：
  - 15 个工具的 `tool_contract`
  - Prompt 分层（System/Tool/Context/User/Schema）
  - fallback 策略
- 固定产出目录：
  - `ai/contracts/`
  - `ai/prompts/`

### 对话 C：Backend 领域模型与工作流编排
- 职责：
  - `Project/AssetDoc/OutputDoc/LockedFact/AIRun/Suggestion/Version`
  - Workflow API、Suggestion 采纳、导出 API
- 固定产出目录：
  - `backend/schema/`
  - `backend/apis/`
  - `backend/workflows/`

### 对话 D：Frontend 工作台与 AI 面板
- 职责：
  - Screen 0/2/3/3B/4/5/6/10 关键交互
  - AI 面板参数、结果预览、采纳动作
- 固定产出目录：
  - `frontend/spec/`
  - `frontend/ui-flows/`

### 对话 E：QA/Eval/发布保障
- 职责：
  - 20 条评测回放
  - 失败分类（taxonomy）
  - 上线 checklist 与回滚演练
- 固定产出目录：
  - `qa/test-cases/`
  - `qa/eval-reports/`
  - `release/`

## 3. 每个对话的统一输入模板（你发给我）
复制下面模板到每个对话第一条消息：

1) 今日目标（只写一个）  
2) 输入边界（允许改哪些文件）  
3) 禁改边界（不能改哪些文件）  
4) 产出路径（必须写到哪个 md/yaml）  
5) 完成标准（DoD）  

## 4. 跨对话“交接协议”
- 所有对话必须在结尾给出：
  1) 今日新增/修改文件清单  
  2) 未完成项（阻塞原因）  
  3) 需要其他对话配合的依赖  
- 主对话每天 22:00 汇总一次，更新总排期。

## 5. 并行优先级（明天可直接开干）
Day 1 并行顺序：
1. 对话 B：先冻结 15 工具合同（最高优先级）
2. 对话 C：按合同建领域模型与 API 草案
3. 对话 D：先做 Screen 6（AI 面板）交互规格
4. 对话 E：先建测试框架与验收模板

## 6. 分支与提交约定
- 分支前缀统一：`codex/`
- 建议分支：
  - `codex/ai-contracts`
  - `codex/backend-workflows`
  - `codex/frontend-workbench`
  - `codex/qa-release`
- 提交信息建议：
  - `feat(ai): ...`
  - `feat(be): ...`
  - `feat(fe): ...`
  - `test(qa): ...`
  - `docs(pm): ...`

