# 金手指 AI 编剧工作台 PRD

## 0. 文档信息
- 模式：Working draft
- 版本：v0.3
- 状态：修订中（甲方/领导浏览版）
- 作者：Codex（基于 2026-03-09 当前讨论与项目材料整理）
- 更新时间：2026-03-09
- 适用团队：甲方 / 领导 / 产品 / 设计 / 前端 / 后端 / AI 算法 / 测试 / 运营

## 1. 一页摘要
金手指本期产品的核心定位，不是一个单纯的 AI 对话写作助手，也不是一个当前即要闭环实现的版权交易平台，而是一款面向职业编剧、编剧团队与短剧公司的 AI 编剧工作台。它以“剧本项目”为核心管理对象，将故事背景、世界观、人物设定、关系网、大纲、分集设计等创作资产，与单集剧本、分镜、剧本介绍、剧本格式稿等创作产出统一组织在一个持续编辑、持续生成、持续沉淀的工作空间中。产品形态参考 Coding IDE 的工作台体验：左侧做项目导航，中间做主编辑区，右侧做上下文化 AI 工具调用区。

更重要的是，金手指不只是在做“写剧本时多几个 AI 按钮”，而是在搭一套面向内容生产的基础设施：项目资产解决单个项目如何持续创作，模板库解决跨项目、跨团队、跨创作者如何持续复用。模板库在正式产品里不是附属资源页，而是可沉淀、可分享、可复用的创作资产网络：官方模板承载平台方法论，团队模板承载组织经验，UGC 模板承载优秀创作者的可复用工作方式。短期它帮助用户更快产出可用剧本，中期它帮助团队沉淀组织级效率资产，长期它与项目资产沉淀、后续版权能力预留一起构成产品差异化。

## 2. 背景与问题定义
### 2.1 背景
从当前项目材料和讨论结果来看，甲方对“金手指”的期待有两个层次。第一层是明确的核心价值：用 AI 辅助剧本创作，提高专业创作者的效率与产出质量；第二层是品牌差异化方向：在完成剧本创作后，平台未来能够帮助用户衔接版权获取、版权确权或版权授权相关能力。基于当前信息，本期应先聚焦第一层，优先把 AI 编剧工作台做扎实，再为后续版权能力预留产品概念与结构接口。

同时，参考材料已经显示出两类信号。其一，甲方已有较明确的模块清单和方法论表达，例如“16 步专业创作法”“AI 工具”“模板库”“提示词库”等；其二，竞品/参考产品“笔花”已经验证了一条较成熟的产品方向，即以创作流程为骨架，以资产沉淀和分阶段 AI 生成作为核心体验。这意味着本期 PRD 不应再把产品设计成单点工具集合，而应明确为一个项目制、工作台式、上下文持续积累的 AI 创作系统。

### 2.2 目标用户
- 主要用户：职业编剧、短剧公司内容策划、编剧工作室成员、内容工厂式创作团队
- 次要用户：制片团队中的内容负责人、项目策划、编剧助理、剧本统筹
- 角色差异：
  - 职业编剧更关注创作连续性、设定一致性、台词和桥段质量，以及自由度
  - 短剧公司更关注批量化生产效率、模板化启动、分集设计速度、交付规范和团队内复用
  - 内容负责人更关注项目状态、阶段推进、质量把控和可交付性

### 2.3 用户问题
当前目标用户的核心任务不是“让 AI 随便生成一段故事”，而是以一个项目为单位，从创意、世界观、人物、大纲、分集设计一路推进到单集剧本、分镜和交付物。这个任务具有明显的长链路、多对象、强上下文、一致性敏感等特点，通用聊天式 AI 工具很难原生适配。

综合现有材料与合理推断，用户当前面临的主要问题如下：
- 用户当前任务：在既定题材、风格、集数、时长和商业目标下，持续完成一个完整短剧项目的策划、写作和交付
- 核心痛点：
  - 设定分散在多个文档和对话中，无法形成持续可引用的项目资产
  - AI 输出一次性强、持续性弱，无法稳定继承前文约束与已确认设定
  - 人物命名、关系、世界观、关键事件、节奏安排容易前后冲突
  - 从资产到产出的切换成本高，用户需要反复复制粘贴 Prompt 和内容
  - 缺少对剧名、钩子、节奏、格式化、分镜等下游交付环节的一体化支持
- 现有替代方案：文档编辑器 + 大模型聊天窗口 + 手工 Prompt 模板 + 人工文件管理 + 零散工具网站
- 影响后果：创作效率低、复用性差、成稿质量不稳定、团队协作成本高、难以形成标准化工作流

### 2.4 证据与判断
当前证据主要来自三部分：第一，甲方提供的“金手指”产品材料；第二，参考产品“笔花”的流程与 Prompt 设计；第三，本轮产品讨论中已经确认的核心定位与 MVP 边界。严格来说，目前仍缺少正式用户访谈和量化数据，因此本 PRD 中的部分判断属于“基于材料和场景的推断”，需要在后续评审中进一步校正。

- 定量证据：暂无正式量化数据
- 定性证据：
  - 已有 PDF 材料清晰列出甲方期望的工具清单与产品包装方向
  - 讨论已明确“写剧本”为核心价值，“版权”为差异化卖点
  - 参考产品证明“项目资产 + 分阶段生成 + 版本化编辑”是成立的产品路径
- 当前假设：
  - 本期优先形态为桌面优先的工作台产品
  - 用户更需要“自由创作 IDE + 推荐流程引导”，而非强制卡步骤的向导式系统
  - 甲方提到的工具需要在完整版本 PRD 中完整覆盖，但首发 MVP 只实现支撑主闭环的核心子集，其余能力在统一框架下分阶段落地
- 还缺什么信息：
  - 甲方对首发平台、部署方式、账号体系、权限模型的要求
  - 是否支持团队协作与多人项目
  - 剧本导出格式的行业标准要求
  - 版权能力未来的真实业务链路和上线时间
  - 预算、模型选择、推理成本和 SLA 要求

## 3. 目标与非目标
本期的核心目标不是追求功能数量最多，而是建立一个结构清晰、体验统一、足以支撑专业创作的项目制工作台。所有功能模块都应服务于两个结果：第一，帮助用户更快启动并持续推进一个剧本项目；第二，帮助用户在 AI 参与写作的过程中保持项目设定与内容产出的一致性。换言之，本期要优先搭建“能写、好写、可持续写”的基础产品骨架。

与此同时，本期必须明确边界。版权能力虽然是产品重要卖点，但其业务流程、法律链路、平台交易规则尚未清晰，不适合作为 MVP 的核心交付目标。类似多人实时协作、复杂权限控制、深度行业数据服务等方向，也应延后，避免稀释主线。

### 3.1 业务目标
本 PRD 的核心，不只是定义一套 AI 写作工具，而是定义一套面向短剧创作场景的内容生产基础设施。对甲方和领导而言，判断它是否值得继续投入，不应只看“单个工具是否可用”，还要看它能否同时完成三个层面的价值累积：第一，提升单个项目的创作效率；第二，沉淀团队可复用的创作资产；第三，为后续模板网络和版权能力预留长期差异化空间。

- 完整产品目标：
  - 覆盖甲方要求的核心工具与文档对象，形成“项目 - 资产 - 产出 - AI 工具 - 模板资产网络 - 导出”的统一工作台
  - 让用户可以在同一项目内完成从创意准备、世界观与人物设定、大纲与分集设计、单集剧本写作、格式整理到提案材料输出的全流程工作
  - 把模板库从冷启动资源页升级为可沉淀、可分享、可复用的创作资产网络，承接官方模板、团队模板和 UGC 模板
  - 通过项目资产沉淀 + 模板复用 + 后续版权能力预留，形成区别于单点 AI 写作工具的长期产品壁垒
- MVP 目标：
  - 先验证用户是否愿意在金手指里完成“创建项目 -> 导入或沉淀核心资产 -> 生成并编辑 1 集剧本 -> 采纳 AI 建议 -> 导出 `.docx` 交付稿”的首条闭环
  - 先交付最影响主闭环的 6 组能力：项目与文档骨架、核心资产、单集剧本编辑、AI 核心工具、采纳与版本、格式化导出
- 业务结果指标（当前为工作 draft 目标值，需在试用中校准）：
  - 试用后继续推进意向率 >= 80%，即甲方在试用后明确进入下一轮原型/开发评审或继续合作讨论
  - 新建项目 7 日内持续创作率 >= 35%，即用户不只试一下，而是愿意继续推进至少一个项目
  - 在试用任务对照中，完成 1 集短剧初稿的中位时长较现有方式下降 >= 30%
- 产品验收指标：
  - 完整产品版必须能承接甲方完整功能清单，不再依赖大量站外工具拼接
  - MVP 必须能在同一工作台内完成 1 集剧本主链路，无需反复复制粘贴 Prompt
  - 模板库必须被定义为正式产品能力，而不是可有可无的资源页，并具备后续扩展为官方模板、团队模板、UGC 模板三层网络的路径
  - AI 生成内容默认不直接覆盖正文，关键交付内容仍保留人工确认权
  - 支持从现有文档导入核心资产或单集剧本文本，并导出标准 `.docx` 剧本稿
- 时间窗口：本期以“完整产品 PRD + 首发 MVP 定义”双交付为目标，具体排期待项目计划确认

### 3.2 用户目标
- 用户希望完成什么：
  - 快速启动一个新的剧本项目，或把已有资料导入为一个结构化项目
  - 在项目内沉淀故事背景、世界观、人物设定、大纲等长期资产
  - 在写单集剧本、分镜、剧本介绍或剧名方案时随时调用 AI 工具辅助创作
  - 对 AI 生成内容进行可控采纳，而不是被动接受整段替换
  - 最终导出更接近行业交付习惯的剧本成果
- 用户获得的核心价值：
  - 更少切换工具与文档
  - 更高的创作连续性与一致性
  - 更高效地完成从策划到剧本再到分镜/提案材料的全链路创作

### 3.3 非目标 / 本期不做
- 不做项 1：版权确权、版权授权交易、版权资产撮合等完整业务闭环
- 不做项 2：复杂团队协作、多人实时编辑、企业级权限体系
- 不做项 3：移动端优先体验
- 不做项 4：完全自主、无需人工审核的一键成片式写作系统
- 原因：
  - 这些方向要么业务规则尚未明确，要么会显著增加工程与交付复杂度
  - 当前阶段应先把 AI 编剧工作台本身做扎实，再考虑扩张到交易、协作和自动化执行

### 3.4 范围分层原则
- 完整产品 PRD 范围：覆盖甲方完整工具清单与相关文档体系，明确每个能力在统一工作台中的角色、对象、入口和交付结果
- MVP 范围：只包含首发必须交付且足以跑通主闭环的能力，重点解决“能不能开始写、能不能持续写、能不能导出交付”
- MVP 不纳入但必须在完整 PRD 中保留定义的能力：扫榜调研深度能力、编剧素质辅助、叙事方法、节奏控制、剧名创作、剧本介绍、分镜深加工、完整项目打包导出等
- 分层原则：MVP 砍的是首发交付范围，不是产品目标；所有被暂缓的能力都必须在完整 PRD 中说明最终形态、默认输出对象和后续接入方式

## 4. 方案概述
本期方案的一句话描述是：以“剧本项目”为中心，构建一个桌面优先、IDE 风格、可持续编辑和上下文化调用 AI 工具的编剧工作台。用户不再围绕一个聊天会话写作，而是在一个项目内管理资产、生产内容、调用工具、审阅结果、沉淀版本，并逐步完成剧本项目。

在产品形态选择上，本期不采用纯向导式流程，也不采用完全自由的空白文档模式，而采用“自由创作 IDE 型为主、叠加推荐流程引导”的方案。这样既能满足专业用户的灵活度，又能保留甲方方法论的结构优势。相比将每个工具拆成单独页面，这种统一工作台方案更利于上下文共享、交互一致性和后续能力扩展。

- 方案一句话描述：项目制 AI 编剧工作台
- 产品整体呈现形式：桌面优先 Web 工作台
- 核心流程概述：创建项目 -> 建立资产 -> 确认关键设定 -> 生成与编辑产出 -> 分析与格式化 -> 导出交付
- 关键价值点：
  - 以项目为单位组织所有创作内容
  - 资产和产出分层管理，结构清晰
  - AI 工具与当前文档强关联，减少复制粘贴 Prompt
  - AI 结果先预览、再采纳，增强可控性
  - 既有推荐流程，也支持自由跳转

### 4.1 工作台总体信息架构
| 区域 | 角色 | 核心内容 | 设计原则 |
| --- | --- | --- | --- |
| 顶部栏 | 全局状态区 | 项目名称、项目元信息、当前阶段、保存状态、导入/导出入口 | 提供项目级总览，不承载深操作 |
| 左侧栏 | 项目导航区 | 项目概览、创作资产、创作产出、参考资料、模板库、提示词库、版本记录 | 以项目对象和阶段结构组织内容 |
| 中间区 | 主编辑区 | 多标签文档编辑、结果对比、版本查看、正文创作 | 用户主要工作发生区域，支持持续编辑 |
| 右侧栏 | AI 工具区 | 与当前文档相关的工具、上下文选择、生成预览、采纳操作 | 上下文化而非独立页面化 |

### 4.2 模板资产网络与长期价值
如果说项目资产解决的是“单个项目怎么持续写”，模板资产网络解决的就是“一个团队、一个公司、一个创作社区如何越写越快”。因此，模板库在金手指里不应只是供用户浏览的资源页，而应是平台长期价值的一部分：它负责承接方法论、沉淀复用经验、放大优秀创作者和团队的产能，并逐步形成平台壁垒。

| 模板类型 | 主要来源 | 核心角色 | 业务价值 | 首发策略 |
| --- | --- | --- | --- | --- |
| 官方模板 | 平台运营、行业专家、甲方方法论 | 冷启动标准供给 | 降低空白页成本，建立统一创作方法与交付标准 | MVP 纳入 |
| 团队模板 | 公司、工作室、项目负责人 | 组织级资产沉淀 | 跨项目复用、培训新人、统一内容标准 | MVP 纳入 |
| UGC 模板 | 优秀创作者、团队公开分享 | 社区级经验扩散 | 形成高质量创作网络，提升平台活跃度与差异化 | 完整产品版重点建设 |

- 价值判断：
  - 对个人创作者，模板库降低启动门槛，让经验可以被复用，而不是每次从零开始
  - 对团队和公司，模板库是组织级效率资产，能把一个项目里验证有效的方法沉淀到下一个项目
  - 对平台本身，模板库是从“工具集合”升级为“创作资产网络”的关键，也是后续连接版权与生态能力的前提

### 4.3 AI 可控性原则
对于甲方和领导，理解这套 AI 方案最关键的不是字段级状态和优先级矩阵，而是四条产品原则：

- 项目上下文优先：AI 优先理解项目内已经沉淀和确认的内容，而不是脱离上下文自由发挥
- 结果先预览后采纳：AI 给出的是建议，不是直接覆盖正文的自动结果
- 上游变化提醒复核：关键信息调整后，系统应提醒用户检查受影响的下游内容
- 关键交付人工确认：单集剧本导出、分镜、对外材料等关键输出保留人工审核与最终判断

更细的对象关系、AI 优先级和冲突处理规则，统一下沉到附录，供后续原型、AI 方案和研发设计使用，不作为甲方/领导浏览版主文重点。

### 4.4 完整版本与 MVP 边界
完整产品版和 MVP 不是两份互相冲突的方案，而是一份方案的两层交付口径。完整产品版回答“这个产品最终要完整长成什么样”，MVP 回答“第一版先交付到什么程度，才足以验证方向且可真正试用”。

| 层级 | 范围定义 | 交付判定 |
| --- | --- | --- |
| 完整产品版 | 覆盖甲方完整工具清单，形成准备、策划、创作、完善全流程；支持资产、产出、工具、模板资产网络、导入导出的一体化管理 | 所有核心工具都有明确入口、对象归属、输出结果与交互口径，模板能力有清晰的官方/团队/UGC 路径 |
| MVP | 跑通“导入/创建项目 -> 核心资产 -> 单集剧本 -> AI 采纳 -> `.docx` 导出”的主闭环；工具只交付核心子集 | 试用用户可在同一工作台完成 1 集剧本闭环，并能感知到一致性控制与 AI 可控性 |
| MVP 后续版本 | 在不推翻工作台骨架的前提下，补齐剩余甲方工具、UGC 模板生态、分镜深加工、提案材料、更多导出格式与更深的分析能力 | 新能力沿用同一工作台骨架、模板资产网络和 AI 可控原则接入 |

## 5. 用户流程
### 5.1 完整产品主流程
1. 进入方式：用户创建新项目，或导入已有世界观、大纲、人物设定、单集剧本等资料，形成一个统一项目容器。
2. 准备阶段：用户通过编剧素质、扫榜调研、模板库等能力完成题材判断、方向收束和项目启动。
3. 策划阶段：用户逐步沉淀世界观、核心卖点、爽点钩子、人物卡、关系网、全剧大纲、分集设计等资产，并对关键事实执行确认/锁定。
4. 创作阶段：用户打开单集剧本或其他产出文档，在主编辑器中持续写作，并按需调用剧情和桥段、开头写作、台词写作、叙事方法、节奏控制等工具。
5. 完善阶段：用户继续生成剧名、剧本介绍、剧本格式稿、分镜等下游交付物，并对外发材料做人工审核。
6. 结果采纳：所有 AI 输出先进入待采纳态，用户可插入、替换、追加为版本、放弃，或将结果回写为新的资产文档。
7. 导出交付：用户按交付对象导出单集剧本、剧本介绍、分镜或项目包，用于外部评审、提案或后续生产。

### 5.2 MVP 首发闭环
1. 用户创建项目，或导入已有创作描述 / 大纲 / 单集剧本文本。
2. 用户补齐并确认至少 3 份核心资产：世界观、人物设定、全剧大纲/分集设计中的任意组合。
3. 用户进入某一集 `OutputDoc`，在主编辑器中持续编辑单集剧本。
4. 用户调用 MVP 核心工具组：世界观设定、核心卖点/钩子提炼、骨骼框架、人物写作、单集剧本助手、剧本格式化。
5. AI 结果进入待采纳态，用户完成插入、替换、追加版本或放弃，并保留版本记录。
6. 用户导出 `.docx` 单集剧本稿，形成可对外评审的最小交付成果。

### 5.3 分支流程 / 异常流程
- 分支场景：
  - 熟练用户可跳过推荐顺序，直接创建单集剧本或导入已有大纲后继续写作
  - 用户可从模板库启动项目，快速预置题材、风格、结构方向
  - 用户可从提示词库调用某类预置 Prompt，辅助当前文档生成
- 失败场景：
  - AI 生成失败、超时或内容明显偏题
  - 用户在缺少关键资产的情况下直接发起下游生成
  - AI 输出与已锁定的人物名、关系、设定冲突
- 回退策略：
  - 保留当前草稿，支持一键重试
  - 缺少关键信息时给出提示并允许继续，但告知输出质量风险
  - AI 输出先进入待采纳态，不直接覆盖正文
  - 用户可查看历史版本并回退

## 6. 功能需求
本期功能拆分遵循“项目骨架优先、统一编辑优先、上下文 AI 优先、工具闭环优先”的原则。也就是说，先搭好一个可持续使用的工作台骨架，再把甲方提出的工具整合到这套骨架里，而不是用大量孤立入口堆砌功能。详细需求分为项目骨架、文档体系、AI 工具体系、结果控制、质量与交付五大块。

### 6.1 功能模块列表
| 模块 | 完整产品版能力 | MVP 是否纳入 | MVP 口径 | 后续补齐 / 增强 |
| --- | --- | --- | --- | --- |
| 项目创建与项目概览 | 支持项目创建、基础信息、阶段状态、推荐下一步 | 是 | 必做 | 后续增加项目模板推荐和项目复制 |
| 现有资料导入 | 支持把已有资料映射为项目资产或产出 | 是 | 支持 `.docx` / `.txt` / 粘贴文本导入核心文档 | 后续支持批量导入、PDF 参考导入、智能拆分 |
| 项目导航与文档树 | 支持资产、产出、参考资料、版本记录统一管理 | 是 | 必做 | 后续增加筛选、标签和批量操作 |
| 资产库管理 | 覆盖世界观、卖点、钩子、人物、关系、大纲、分集设计等全部资产文档 | 是 | MVP 默认重点保证世界观、人物、大纲/分集设计 | 后续补齐更多结构化字段和交叉引用 |
| 产出区管理 | 覆盖单集剧本、分镜、剧名方案、剧本介绍、格式稿等文档 | 是 | MVP 先保证单集剧本与格式稿 | 后续补齐分镜、剧名、剧本介绍等完整产物 |
| 主编辑器与版本 | 支持多标签、自动保存、版本对比、回退、冲突提醒 | 是 | 必做 | 后续增加更强的 diff、批注和对照模式 |
| AI 工具面板 | 统一承载甲方全部工具，按当前文档动态推荐 | 是 | 先交付核心工具子集 | 后续补齐剩余工具并增加更深分析能力 |
| 模板资产网络与提示词库 | 提供官方模板、团队模板、UGC 模板与任务型 Prompt 资源 | 是 | MVP 先提供官方模板与团队/个人模板复用 | 后续补齐 UGC 发布、推荐、审核与激励能力 |
| 一致性检查与质量分析 | 覆盖锁定事实冲突、节奏问题、结构问题、质量提示 | 部分 | MVP 先做锁定事实冲突提示和基础格式检查 | 后续补齐节奏分析、叙事诊断、质量评分 |
| 格式化与导出 | 支持标准格式整理和外发文件导出 | 是 | MVP 强制支持 `.docx` 剧本导出 | 后续支持 `.pdf`、`.xlsx`、项目包导出 |

#### 6.1-A MVP 判定标准
- MVP 不是“只做骨架”，而是必须同时满足以下 5 项：
  - 能创建项目并生成最小可用文档树
  - 能导入已有资料或手动沉淀核心资产
  - 能在单集剧本文档中连续编辑并调用核心 AI 工具
  - 能完成 AI 建议采纳、版本回溯和关键冲突提示
  - 能导出 `.docx` 单集剧本稿用于外部评审
- 任何不能支撑上述闭环的能力，都不应被算作 MVP 必做项，即使它在完整产品版中属于高价值能力

#### 6.1-B 完整产品版判定标准
- 完整产品版需要覆盖甲方完整工具清单，而不是停留在 MVP 的 6 组核心能力
- 所有新增工具必须继续复用同一项目资产机制、AI 可控规则和统一工具面板
- 完整产品版允许分期交付，但 PRD 中必须把最终能力定义、默认输出对象和接入方式写清楚

### 6.2 详细需求
#### R0. 统一项目资产与 AI 可控机制
- 用户角色：甲方、产品、设计、研发共同遵守的基础约束
- 触发条件：进入产品方案、原型、AI 方案与研发设计阶段
- 前置条件：无
- 功能描述：
  - 系统围绕“项目 / 创作资产 / 创作产出 / AI 建议 / 版本”组织，而不是围绕单次聊天组织
  - 关键资产可被人工确认，用于支撑下游生成、编辑和复核
  - AI 结果必须先预览后采纳，导出内容必须可以回溯到当前项目版本
- 规则与约束：
  - 任何新增工具都不能绕开项目资产体系单独存在
  - 上游关键信息变化时，应提醒用户复核受影响内容
  - 更细的对象关系、优先级和状态语义进入附录，不作为甲方/领导浏览版主文重点
- 正常结果：产品方向清楚，后续设计与研发都围绕统一项目资产机制展开
- 异常/边界：
  - 若新增能力无法纳入统一工作台与项目资产机制，需回到方案层重新评估
- 埋点/数据建议：项目资产创建量、AI 建议采纳率、复核提醒触发次数

#### R1. 项目创建与基础信息管理
- 用户角色：职业编剧、策划、内容负责人
- 触发条件：用户点击“新建项目”或从模板创建项目
- 前置条件：用户已登录系统
- 功能描述：
  - 用户可创建剧本项目并填写项目名称、剧本类型、风格、目标集数、单集时长、创作描述等基础字段
  - 系统为每个项目自动生成默认的资产区与产出区结构
  - 系统在项目首页展示项目元信息、阶段进度、最近编辑文档、推荐下一步
- 规则与约束：
  - 项目名称必填
  - 集数、时长等字段支持后续编辑，但修改后需提示可能影响下游内容
  - 新建项目后必须至少生成一套默认文档骨架
- 正常结果：用户进入已初始化完成的项目工作台
- 异常/边界：
  - 用户中途退出时保留已填写信息
  - 当项目已存在大量下游内容时修改基础规格，需二次确认
- 埋点/数据建议：项目创建成功率、创建后 10 分钟内进入首个文档的比例、模板创建占比

#### R1-A. 现有资料导入
- 用户角色：职业编剧、策划、内容负责人
- 触发条件：用户在项目创建时或进入项目后点击“导入资料”
- 前置条件：用户已登录，且已创建项目或正在创建项目
- 功能描述：
  - 支持把已有 `.docx`、`.txt` 或粘贴文本导入到创作描述、世界观、人物设定、全剧大纲、分集设计、单集剧本文档中
  - 用户导入时必须选择目标文档类型；若目标文档不存在，系统自动创建对应文档节点
  - 系统保留原始导入文件快照，并生成导入摘要，便于后续追溯
- 规则与约束：
  - MVP 只要求支持单文件导入与手动映射；批量导入、PDF 解析、自动拆分到多文档属于后续版本
  - 导入内容默认处于 `draft` 状态，不直接变成 `confirmed` 或 `locked`
  - 若导入内容与现有锁定事实冲突，系统需提示冲突并允许用户选择保留现状、创建新版本或覆盖后重算依赖
- 正常结果：用户能把已有创作资料无缝带入工作台，而不是被迫从零开始
- 异常/边界：
  - 文件解析失败时保留原文件记录并提示重新上传或粘贴文本
  - 超长导入内容需支持截断预览和分段落库
- 埋点/数据建议：导入成功率、导入后继续编辑率、导入类型分布、导入冲突率

#### R2. 项目导航与文档树
- 用户角色：所有创作用户
- 触发条件：进入项目工作台
- 前置条件：项目已存在
- 功能描述：
  - 左侧文档树按“项目概览 / 创作资产 / 创作产出 / 参考与资源 / 版本记录”组织
  - 每个文档节点展示名称、状态、最近更新时间，必要时展示完成度或提醒
  - 支持快速定位当前文档，并在不同文档间切换
- 规则与约束：
  - 创作资产与创作产出必须分层显示
  - 已确认资产需有清晰标识
  - 单集剧本和分镜应支持按集数分层展示
- 正常结果：用户能清晰理解项目结构并快速跳转
- 异常/边界：
  - 当文档数量较多时，需要避免导航混乱
  - 空项目应提供空态说明和推荐入口
- 埋点/数据建议：文档切换频次、资产文档与产出文档访问占比、导航跳出率

#### R3. 创作资产库管理
- 用户角色：编剧、策划、内容负责人
- 触发条件：用户进入资产类文档，或调用资产生成工具
- 前置条件：项目已创建
- 功能描述：
  - 系统提供以下资产类文档：故事背景、世界观设定、核心卖点、爽点和钩子、主要角色、次要角色、人物关系、人物弧光、全剧大纲、分集设计
  - 用户可在每份资产中手动编辑，或调用 AI 生成初稿、修订、重写、补充
  - 用户可对资产执行“确认/锁定”，作为后续产出生成的优先事实来源
- 规则与约束：
  - 资产类文档应支持持续编辑，不得仅以只读卡片形式存在
  - 当已锁定资产被修改时，应提示下游内容可能受影响
  - 角色名、关系、关键设定应尽量结构化保存，便于后续引用
- 正常结果：项目形成一套可持续复用的上游资产
- 异常/边界：
  - 缺少部分资产时，系统允许继续，但需提示风险
  - 若 AI 输出与锁定资产冲突，需明确标出冲突项
- 埋点/数据建议：各资产文档创建率、锁定率、AI 辅助生成使用率、资产后续被引用次数

#### R4. 创作产出区管理
- 用户角色：编剧、策划、内容负责人
- 触发条件：用户进入产出类文档，或基于资产发起生成
- 前置条件：项目已存在，建议已有部分资产
- 功能描述：
  - 系统提供以下产出类文档：单集剧本、分镜、剧本格式稿、剧本介绍、剧名方案
  - 单集剧本与分镜应支持按集数创建和管理
  - 产出文档调用 AI 时可自动引用已确认的资产上下文
- 规则与约束：
  - 产出类文档必须可编辑、可保存、可版本化
  - 分镜生成必须基于已有剧本文本，不允许无剧本直接生成完整分镜成稿
  - 当依赖资产不足时，系统应提示但不强制阻断
- 正常结果：用户可从项目资产平滑过渡到具体稿件交付
- 异常/边界：
  - 若单集剧本内容为空，分镜工具应提示先产出剧本或导入内容
  - 若集数规格变化，已有单集内容需保留但提示用户检查受影响范围
- 埋点/数据建议：首个单集剧本生成率、分镜生成率、剧本介绍/剧名工具使用率

#### R5. 主编辑器与多标签工作区
- 用户角色：所有创作用户
- 触发条件：打开任意文档
- 前置条件：项目已存在
- 功能描述：
  - 中间主编辑区支持多标签并行打开多个文档，例如“大纲”“人物设定”“第 12 集剧本”
  - 支持自动保存、手动保存、未保存状态提醒
  - 支持查看当前文档版本、对比 AI 建议与正文差异
- 规则与约束：
  - 主编辑器必须是本产品的核心工作区域，不应退化为只读展示区
  - 文档切换时需保留未提交草稿，避免内容丢失
  - 对于已锁定资产，编辑时应提示“修改将影响下游引用”
- 正常结果：用户在一个工作台中持续完成创作，而不是频繁跳页
- 异常/边界：
  - 网络异常时需尽量保留本地草稿或提供恢复提示
  - 大文本内容编辑性能需满足基本可用性
- 埋点/数据建议：单项目平均打开标签数、自动保存触发次数、版本对比使用率

#### R6. AI 工具面板与甲方工具清单实现
- 用户角色：所有创作用户
- 触发条件：在当前文档中点击右侧 AI 工具
- 前置条件：当前项目存在，且至少有当前文档
- 功能描述：
  - 右侧工具面板根据当前文档类型和项目阶段动态推荐工具，而非给出无差别全量列表
  - 工具调用时可自动注入当前文档、已确认资产、锁定事实、用户选中内容、项目基础信息等上下文
  - 完整产品版需覆盖甲方要求的全部工具，并通过统一交互框架呈现；MVP 先交付支撑主闭环的核心工具子集
- 规则与约束：
  - 每个工具都必须定义输入来源、前置条件、默认输出目标、是否进入待采纳态，以及与锁定事实的关系
  - AI 工具不得默认直接覆盖正文
  - 对强依赖前置内容的工具，应在入口前给出提示，并说明缺失上下文会造成的影响
- 正常结果：用户在不离开当前工作区的前提下调用 AI 完成提效，且所有工具都共享一致的可控机制
- 异常/边界：
  - AI 返回失败时保留当前文本并支持重试
  - 如果引用内容缺失，应给出“将按当前已有信息生成”的提示
  - 若结果与高优先级事实冲突，需在建议结果层给出冲突标签，而不是直接写回正文
- 埋点/数据建议：工具调用次数、分工具采纳率、单次生成耗时、失败率、冲突率

##### R6-A. 甲方工具分组、完整版定义与 MVP 边界
| 阶段 | 工具 | 完整版能力定义 | MVP 是否纳入 | MVP 口径 | 默认输出目标 |
| --- | --- | --- | --- | --- | --- |
| 准备阶段 | 编剧素质 | 提供创作准备清单、常见问题提醒、方法论指引与自检建议 | 否 | 不进入首发闭环 | 右侧建议卡 / 项目备忘 |
| 准备阶段 | 扫榜调研 | 支持对题材、结构、爆点要素和对标内容做辅助整理与归纳 | 否 | 仅保留产品定义，不进 MVP | 调研记录 / 参考资料 |
| 策划阶段 | 世界观设定 | 支持从创意生成、改写、补全世界观与规则设定 | 是 | MVP 核心工具之一 | 世界观文档 |
| 策划阶段 | 提炼核心卖点 | 从创意或项目描述中提炼冲突、爽点、差异化卖点 | 是 | MVP 核心工具之一 | 核心卖点文档 |
| 策划阶段 | 爽点和钩子 | 生成开头钩子、反转点、情绪递进与高爽节点建议 | 合并纳入 | MVP 中并入“核心卖点 / 单集剧本助手”能力包 | 爽点和钩子文档 / 当前文档建议 |
| 策划阶段 | 骨骼框架 | 生成三幕式或结构化全剧大纲，并支持分集拆分 | 是 | MVP 核心工具之一 | 全剧大纲文档 |
| 创作阶段 | 剧情和桥段 | 为当前大纲或单集剧本补充关键桥段、冲突升级和转折设计 | 是 | MVP 核心工具之一 | 当前大纲 / 剧本文档 |
| 创作阶段 | 人物写作 | 生成人物卡、弧光、角色强化建议与关系补充 | 是 | MVP 核心工具之一 | 角色文档 |
| 创作阶段 | 叙事方法 | 提供叙事结构、时间线、视角组织和表现手法建议 | 否 | 先不纳入首发 MVP | 当前文档建议 |
| 创作阶段 | 开头写作 | 为当前剧本生成不同风格的开篇方案和首场戏建议 | 合并纳入 | MVP 中并入“单集剧本助手”能力包 | 当前文档建议 / 剧本文档 |
| 创作阶段 | 台词写作 | 优化台词口语化、人物口吻、情绪表达与节奏感 | 是 | MVP 核心工具之一 | 当前剧本文档 |
| 创作阶段 | 节奏控制 | 分析节奏分布、钩子密度、转折间距并给出建议 | 否 | MVP 仅保留基础一致性提醒，不做独立工具 | 节奏分析面板 / 当前文档建议 |
| 完善阶段 | 剧名创作 | 生成多版备选剧名并支持风格筛选 | 否 | 不进入首发闭环 | 剧名方案文档 |
| 完善阶段 | 剧本格式 | 将剧本文本整理为标准格式并支持导出 | 是 | MVP 核心工具之一 | 剧本格式稿 |
| 完善阶段 | 剧本介绍 | 生成宣传简介、提案摘要和对外介绍文案 | 否 | 完整版能力，MVP 不纳入 | 剧本介绍文档 |

- MVP 核心工具组定义：
  - 1）世界观设定
  - 2）提炼核心卖点（含基础钩子能力）
  - 3）骨骼框架
  - 4）人物写作
  - 5）单集剧本助手（由剧情和桥段 + 开头写作 + 台词写作组成）
  - 6）剧本格式
- 完整版要求：上述所有工具都需要在统一工作台中具备明确入口、输入前提、输出对象和采纳机制，不能只停留在“能力名称列表”。

#### R7. AI 结果采纳机制
- 用户角色：所有创作用户
- 触发条件：AI 工具生成结果后
- 前置条件：已有 AI 输出
- 功能描述：
  - AI 输出进入“待采纳态”，支持以下操作：插入到光标处、替换选中内容、追加为新版本、复制到剪贴板、放弃
  - 对较长内容，系统提供差异预览或对比视图
  - 用户可将某次 AI 结果保存为文档版本，便于回溯
- 规则与约束：
  - 不允许默认无感覆盖正文
  - 追加为新版本时需记录触发工具与时间
  - 当用户选中部分文本发起改写时，应优先作用于选中范围
- 正常结果：用户对 AI 输出有充分控制权
- 异常/边界：
  - 若用户在结果生成期间切换文档，系统需提示结果归属
  - 若当前文档被删除，结果仍应可临时保留但不能写回无效对象
- 埋点/数据建议：采纳率、放弃率、替换/插入/追加版本的行为占比

#### R8. 推荐流程、进度与状态管理
- 用户角色：所有创作用户
- 触发条件：进入项目概览页或文档页
- 前置条件：项目已创建
- 功能描述：
  - 系统按“准备阶段 / 策划阶段 / 创作阶段 / 完善阶段”给出推荐流程
  - 展示每一阶段的完成情况、当前建议下一步、关键缺失项提示
  - 用户可自由跳转，不被强制卡死
- 规则与约束：
  - 推荐流程是引导，不是阻断逻辑
  - 阶段完成度算法应以文档是否存在、是否有内容、是否已确认为主要依据
- 正常结果：新手用户能得到方法论指导，熟练用户仍有自由度
- 异常/边界：
  - 当用户逆序创作时，不应报错，只需给出提醒
  - 若关键资产为空，系统需在下游工具入口标明“建议先补齐”
- 埋点/数据建议：推荐步骤点击率、跨阶段跳转率、阶段完成率

#### R9. 模板资产网络与提示词库
模板库在金手指里不是“多一个导航页”，而是产品从工具走向平台的关键抓手。项目资产解决单个项目的连续创作问题，模板资产网络解决跨项目、跨团队、跨创作者的经验复用问题；这也是金手指未来形成组织级效率资产和社区壁垒的关键。

- 用户角色：新手用户、批量生产团队、熟练创作者、内容负责人
- 触发条件：用户在项目创建时或创作过程中打开模板/提示词入口
- 前置条件：系统已提供预置模板和 Prompt 资源
- 功能描述：
  - 模板库按“官方模板 / 团队模板 / UGC 模板”三层组织，不同模板承担不同复用价值
  - 官方模板用于项目冷启动，承接平台方法论、行业套路和标准交付结构
  - 团队模板用于公司或工作室沉淀跨项目可复用资产，例如角色模板、题材模板、结构模板、交付模板
  - UGC 模板用于优秀创作者公开分享创作方法、结构经验和高质量范式，形成平台级创作网络
  - 提示词库支持按任务类型浏览，例如故事梗概、人物小传、分集设计、单集剧本、台词优化、剧名生成、剧本介绍等
  - 用户可将模板或 Prompt 应用于当前项目或当前文档，并可将成熟做法沉淀为个人/团队模板
- 规则与约束：
  - 模板和提示词应作为提效与复用资产，不替代项目资产结构
  - 发布到团队模板或 UGC 模板库的内容需包含作者/来源、适用场景、复用说明和基本审核信息
  - UGC 模板需预留审核、署名、推荐和下架机制；MVP 先以官方模板与团队/个人模板复用为主，UGC 发布与推荐纳入完整产品版
  - 使用模板时需提示用户哪些字段将被预填或覆盖
- 正常结果：用户不仅能更快启动项目，还能把有效方法沉淀为可持续复用的创作资产
- 异常/边界：
  - 当模板与当前项目题材差异较大时，应提醒可能不匹配
  - 当模板存在版权争议、抄袭投诉或质量不达标时，应支持下架或限制推荐
- 埋点/数据建议：模板启动项目占比、跨项目模板复用率、团队模板沉淀率、UGC 模板贡献率、提示词库打开率

#### R10. 格式化、导出与交付
- 用户角色：编剧、内容负责人、策划
- 触发条件：用户完成阶段性内容后点击格式化或导出
- 前置条件：已有剧本文本或其他可导出内容
- 功能描述：
  - 支持将剧本文本整理为标准展示格式，并保留原始内容语义不变
  - MVP 必须支持单集剧本导出为 `.docx`；完整产品版继续支持剧本介绍导出为 `.docx` / `.pdf`，分镜导出为 `.xlsx` 或表格型 `.docx`
  - 导出前允许用户选择导出对象，例如单集剧本、剧本介绍、分镜、全项目资料包等
  - 导出文件需带上项目名称、集数、版本号、导出时间等元信息，便于外部流转和评审
- 规则与约束：
  - 格式化应保留原文内容，不擅自改写剧情含义
  - 导出动作必须绑定当前导出版本，保证外发文件可追溯
  - 若用户导出的是存在待复核提醒的文档，系统需提示“上游关键信息已变更，建议先复核”
- 正常结果：用户能形成可外发、可评审、可追溯的阶段性成果
- 异常/边界：
  - 当内容为空时不允许导出成品，但允许提示后继续导出空模板
  - 当格式化失败时保留原文并提示重试
  - 当目标格式暂不支持时，系统需清楚说明可用格式和预计后续支持范围
- 埋点/数据建议：导出次数、导出成功率、导出对象类型占比、导出前待复核提示触发率

## 7. 体验与交互要求
本期体验的核心，不是做一个“大而全的工具目录”，而是做一个“可以一直待在里面写”的工作台。因此交互上必须让用户感觉：所有内容都是这个项目的一部分，所有 AI 能力都是围绕当前工作而不是围绕聊天本身展开。设计上应优先保证结构清晰、跳转少、上下文连续、结果可控。

### 7.1 信息架构
- 顶部：项目名称、项目基础信息入口、阶段进度、保存状态、导出入口
- 左侧：
  - 项目概览
  - 创作资产
  - 创作产出
  - 模板库
  - 提示词库
  - 参考资料
  - 版本记录
- 中间：多标签编辑器、差异对比、版本预览
- 右侧：AI 工具、上下文来源说明、生成结果、采纳操作

### 7.2 关键页面 / 关键卡片
- 项目概览页：展示项目基础信息、阶段进度、最近编辑文档、推荐下一步
- 资产文档页：正文编辑 + AI 工具调用 + 确认锁定状态
- 单集剧本页：正文编辑 + 台词/开头/桥段/节奏工具 + 分镜生成入口
- 分镜页：基于剧本内容生成分镜表，并支持人工编辑
- 模板库页：模板检索、预览、应用到当前项目
- 提示词库页：按任务浏览、查看、复制、应用

### 7.3 空态
- 新建项目后，空态应直接告诉用户下一步能做什么，例如“先补充创作描述”或“从模板启动”
- 空文档页应提供“手动写作”和“AI 生成初稿”两个明确入口
- 分镜空态应提示“先完成单集剧本再生成分镜”

### 7.4 加载态
- AI 生成中需展示当前工具名称、预计生成对象和上下文来源
- 长生成需允许用户取消，不应阻塞正文编辑
- 多步骤工具可展示阶段式进度文案，减少等待焦虑

### 7.5 错误态
- 生成失败时必须保留当前原文与用户输入
- 错误提示需区分网络失败、服务失败、上下文不足、内容冲突等类型
- 当 AI 内容疑似与锁定资产冲突时，应提示冲突项而非只显示“生成失败”

### 7.6 权限差异
- 本期默认以单用户项目为主，不设计复杂权限模型
- 若后续需要多人协作，可在项目层扩展 owner/editor/viewer 等角色
- 当前 PRD 不将协作权限作为本期交付项

## 8. 指标设计
### 8.1 业务成功指标
当前没有历史基线，因此以下目标值属于工作 draft 目标，需在甲方试用和小范围灰度中校准。对甲方和领导而言，真正重要的不是用户点了多少次按钮，而是这个产品有没有帮助团队更高效地产出内容、沉淀可复用资产，并形成继续推进的商业价值。

| 指标 | 定义 | 目标值 | 适用层级 |
| --- | --- | --- | --- |
| 试用后继续推进意向率 | 试用结束后，甲方明确进入下一轮原型/开发评审或继续合作讨论的比例 | >= 80% | MVP / 完整版 |
| 7 日持续创作率 | 新建项目后 7 日内至少二次进入项目并继续编辑的项目占比 | >= 35% | MVP / 完整版 |
| 单集剧本完成时长下降 | 在相同试题下，相比原有“文档 + 聊天窗口”流程，完成 1 集初稿的中位时长降幅 | >= 30% | MVP |
| 完整工具覆盖率 | 甲方要求的工具中，已在统一工作台中具备明确入口和交互定义的能力占比 | 100%（PRD 定义层）/ >= 80%（产品实现层） | 完整版 |

### 8.2 模板生态指标
- 指标名：模板带动项目启动率
  - 定义：通过官方模板、团队模板或 UGC 模板启动的新项目占比
  - 目标值：>= 30%
- 指标名：跨项目模板复用率
  - 定义：被至少两个项目重复使用的模板占已使用模板总数的比例
  - 目标值：>= 25%
- 指标名：团队模板沉淀率
  - 定义：团队完成项目后，将其中有效结构、设定或交付格式沉淀为可复用模板的项目占比
  - 目标值：>= 20%
- 指标名：UGC 模板贡献率
  - 定义：进入完整产品版后，活跃创作者中至少发布 1 个公开模板的账号占比
  - 目标值：>= 10%（完整产品版观察目标）

### 8.3 MVP 验证指标
- 指标名：首发闭环完成率
  - 定义：完成“创建/导入 -> 关键上下文准备 -> 单集剧本 -> AI 采纳 -> `.docx` 导出”的项目占比
  - 目标值：>= 40%
- 指标名：关键上下文准备达标率
  - 定义：在进入单集剧本写作前，项目已通过手动编辑、导入资料或模板复用等方式补齐足以支撑当前创作的关键上下文的项目占比
  - 目标值：>= 60%
- 指标名：AI 核心工具采纳率
  - 定义：MVP 核心工具产生的 AI 建议中，最终被插入 / 替换 / 追加版本的占比
  - 目标值：>= 45%
- 指标名：导入后继续创作率
  - 定义：导入资料后的项目中，用户继续编辑至少 1 个文档并触发 1 次 AI 工具的项目占比
  - 目标值：>= 50%

### 8.4 护栏指标
- 指标名：AI 工具调用失败率
  - 不允许恶化的阈值：<= 5%
- 指标名：AI 生成 P95 响应时长
  - 不允许恶化的阈值：<= 20 秒
- 指标名：AI 结果生成后 10 分钟内被完全删除或回退的比例
  - 不允许恶化的阈值：<= 35%
- 指标名：导出后发现严重事实冲突的比例
  - 不允许恶化的阈值：<= 10%
- 指标名：文档内容丢失事故数
  - 不允许恶化的阈值：0

## 9. 依赖、风险与上线
### 9.1 依赖
- 设计依赖：
  - 工作台信息架构与页面布局设计
  - 编辑器交互、差异对比、状态标识、工具面板设计
- 技术依赖：
  - 富文本或长文本编辑器能力
  - 文档存储、版本管理、自动保存机制
  - AI 调用编排层与上下文拼装能力
  - 导出与格式化能力
- 数据依赖：
  - 模板库内容
  - 提示词库内容
  - 各工具的默认规则与能力边界定义
- 外部依赖：
  - 甲方确认工具优先级、名称与对外表述
  - 甲方后续澄清版权能力的业务实现方式

### 9.2 风险
| 风险 | 影响 | 概率 | 应对 |
| --- | --- | --- | --- |
| 甲方工具清单较多，范围膨胀 | 高 | 高 | 用统一工作台和统一 AI 调用框架承载工具，先保证入口与基本闭环，避免逐个做重页面 |
| AI 输出质量不稳定，导致用户不信任 | 高 | 中 | 强化上下文注入、锁定资产机制、结果待采纳态、版本回退 |
| 项目资产关系不清，导致主线理解和 AI 引用混乱 | 高 | 中 | 在设计和研发前先冻结项目资产机制与文档树结构 |
| 模板生态质量与版权归属不清，影响复用和平台信任 | 高 | 中 | 区分官方/团队/UGC 模板，明确署名、审核、推荐与下架规则 |
| 编辑器与 AI 面板交互复杂，影响可用性 | 中 | 中 | 先定义最小闭环：编辑、生成、对比、采纳、保存 |
| 节奏分析、扫榜调研等能力实现深度不明 | 中 | 高 | 在 PRD 中标注可先轻量实现，后续迭代深化 |
| 版权卖点过早进入主流程，分散用户认知 | 中 | 中 | 本期仅在定位与宣传层保留，不纳入主工作台核心流程 |

### 9.3 上线与验证
- 上线方式：建议先白名单 / 小范围灰度给甲方及内部试用用户
- 回滚条件：
  - 项目创建或保存异常显著影响主流程
  - AI 工具失败率持续超阈值
  - 文档内容丢失或版本异常
- MVP 验证清单：
  - 用户可成功创建项目并进入默认文档骨架
  - 用户可导入已有资料或创建核心资产，并形成可用于写作的关键上下文
  - 至少一套从资产到单集剧本再到 `.docx` 导出的链路可跑通
  - AI 结果可预览、采纳、回退，且冲突提示可见
  - 模板库、提示词库、格式化、导出可正常使用
- 完整产品版补充验证清单：
  - 甲方完整工具清单均有明确入口与默认输出对象
  - 分镜、剧名、剧本介绍等非 MVP 产物已接入同一工作台机制
  - 新增工具继续复用同一工作台骨架、模板策略与 AI 可控规则

## 10. 已冻结决策与开放问题
### 10.1 当前冻结决策（按本版 PRD 执行）
- 首发形态：桌面优先 Web 工作台；如后续需要桌面壳层，原则上不改变工作台布局与项目资产主线
- 范围口径：本 PRD 覆盖甲方完整功能清单，同时明确首发 MVP 只交付主闭环核心子集
- 模板策略：正式产品按“官方模板 / 团队模板 / UGC 模板”三层设计；MVP 先交付官方模板与团队/个人复用，UGC 发布与推荐纳入完整产品版
- 导入口径：MVP 支持 `.docx`、`.txt` 和粘贴文本导入核心资产及单集剧本；批量导入、PDF 参考导入属于后续版本
- 导出口径：MVP 强制支持单集剧本 `.docx` 导出；完整产品版补齐剧本介绍 `.docx` / `.pdf`、分镜 `.xlsx` / 表格型 `.docx` 和项目包导出
- AI 控制口径：项目上下文优先、AI 建议先预览后采纳、上游关键信息变更触发复核提醒

### 10.2 仍需甲方确认的开放问题
- 待确认问题 1：扫榜调研需要接入什么数据源，首发是人工整理辅助还是自动抓取分析？
- 待确认问题 2：分镜导出是否有指定行业模板，若没有则按通用表格模板先行
- 待确认问题 3：剧本介绍、剧名创作等完善阶段能力，对外评审时是必须项还是加分项？
- 待确认问题 4：项目是否存在多人参与、审批、提审等协作场景，以及何时进入版本规划
- 待确认问题 5：甲方对模型成本、速度、内容安全和版权合规是否有明确上限要求
- 待确认问题 6：版权能力在 MVP 阶段对外展示到什么程度最合适，是否需要保留占位入口

### 10.3 建议的下一步
- 先基于本 PRD 冻结工作台信息架构、模板策略和 MVP 核心工具组
- 再补一份原型级功能说明，细化页面结构、文档树、工具面板和关键状态
- 同步补一份 AI 方案设计，明确各工具的上下文来源、冲突检测与评估集；详细对象关系和优先级规则进入内部方案附录

## 11. AI 补充章节（仅 AI 需求需要）
### 11.1 模型负责什么
- 基于项目资产和当前文档生成初稿、续写、改写和补充内容
- 帮助提炼核心卖点、钩子、人物特征、桥段、台词、节奏建议等
- 将上游资产在下游产出中持续引用，提升内容一致性
- 将剧本文本转换为分镜或标准格式草稿

### 11.2 模型不负责什么
- 不负责替代用户进行最终创作决策
- 不负责保证法律、版权、市场判断的真实性或有效性
- 不负责在无人工确认的情况下自动发布最终剧本成果
- 不负责完整的版权确权、交易与法律合规闭环

### 11.3 AI 可控性原则
对甲方和领导来说，理解金手指的 AI 能力，重点不在于字段级规则，而在于它是否可控、可解释、可复核。主文只保留以下四条原则：

- 项目内已确认的上下文优先于开放式生成
- AI 输出先预览、后采纳，不直接覆盖正文
- 上游关键信息变更时，系统提醒复核受影响内容
- 对外材料、关键导出和高风险内容保留人工最终确认

更细的事实优先级与冲突处理详版见附录 C，供内部方案和研发设计参考。

### 11.4 错误兜底与降级策略
- AI 结果默认进入待采纳态，不直接覆盖正文
- 当关键信息不足时，提示“基于当前已有信息生成，结果可能不稳定”，并允许继续
- 当内容与锁定事实冲突时，优先给出冲突提醒，并允许用户查看冲突来源
- 模型或服务失败时，支持重试、改写、放弃、回滚历史版本，并保留本次输入与上下文快照
- 对于无法稳定生成的工具，系统可降级为模板建议、结构提纲或人工编辑指引，而不是返回空白失败

### 11.5 人工审核点
- 核心资产从 `draft` 升级为 `confirmed` / `locked` 前需用户人工检查
- 单集剧本导出前需用户人工确认当前版本
- 分镜稿、剧本介绍等对外材料生成后需用户人工校对
- 所有涉及版权、市场判断、对外宣传的材料必须人工审核后方可使用

### 11.6 评估计划
- 离线评估样本：
  - 以 20 个短剧项目样本作为基线，覆盖现代、古风、悬疑、甜宠等题材
  - 每个项目至少抽取 3 类任务：资产生成、单集剧本写作、格式化/介绍类生成
  - MVP 阶段重点评估 6 组核心工具；完整产品版再扩展到全部工具
- 评分维度与权重：
  - 一致性（30%）：人物、关系、世界观、时间线是否与高优先级事实一致
  - 可用性（25%）：结果是否能直接被采纳或少量修改后采纳
  - 可控性（20%）：结果是否易于比较、采纳、回滚，是否清楚说明上下文来源
  - 格式合规（15%）：是否满足剧本或交付模板的结构要求
  - 表达质量（10%）：台词自然度、桥段表达、钩子吸引力等
- 标注方式：
  - 由产品经理 + 资深编剧双人评分，采用 1-5 分量表
  - 若两人分差 > 1 分，进入复审并记录失败原因 taxonomy
- MVP 离线准入阈值：
  - 加权平均分 >= 3.8 / 5
  - 人名、关系、关键设定冲突率 <= 5%
  - 输出可采纳率 >= 40%
  - 格式合规率 >= 90%
- 在线评估：
  - 闭环完成率、AI 核心工具采纳率、导入后继续创作率、7 日持续创作率、导出成功率
- 失败分类：
  - 事实冲突、上下文不足、结构跑偏、语言质量差、格式不合规、响应失败、用户误操作

## 附录 A：当前已确认产品判断
- 金手指的核心价值是辅助写剧本，不是当前阶段的版权交易平台
- 版权能力是差异化卖点，但不进入本期开发范围
- 工作台结构参考 Coding IDE：左导航、中编辑、右工具，完整产品版与 MVP 共享同一骨架
- 中间编辑对象不是单篇长文，而是一个包含资产和产出的剧本项目
- 产品形态以“自由创作 IDE 型”为主，叠加“推荐流程引导”；MVP 只缩首发范围，不改变最终产品形态
- 模板库在正式产品中被定义为创作资产网络，而不是附属资源页；其长期目标包括官方模板、团队模板和 UGC 模板三层能力

## 附录 B：内部工作说明 - 项目资产关系草案
以下内容供后续原型、AI 方案和研发设计参考，不作为甲方/领导浏览版主文重点。

| 对象 | 定义 | 核心字段 | 关键状态 |
| --- | --- | --- | --- |
| Project | 一个完整的剧本项目容器，承载全部资产、产出、版本和 AI 记录 | 项目名称、题材、风格、目标集数、单集时长、创作描述、阶段、状态 | draft / active / archived |
| AssetDoc | 上游创作资产文档，如世界观、人物、关系、大纲、分集设计 | 类型、正文、结构化字段、来源、确认状态、最近版本 | empty / draft / confirmed / locked / needs_review |
| OutputDoc | 下游产出文档，如单集剧本、分镜、剧名方案、剧本介绍、格式稿 | 类型、集数、正文、依赖资产、导出状态、最近版本 | empty / draft / in_progress / review_ready / exported / needs_review |
| LockedFact | 从已确认资产中提炼出的高优先级事实项，是 AI 的主要 source of truth | fact_key、fact_value、source_doc、source_version、owner、更新时间 | active / superseded |
| AIRun | 一次 AI 工具执行记录，记录工具、上下文快照、输出和耗时 | tool_id、target_doc、context_snapshot、status、latency、error_code | pending / running / succeeded / failed / cancelled |
| Suggestion | 一次 AI 输出结果，可被采纳、放弃、追加为版本或标记冲突 | run_id、target_range、diff、conflict_tags、status | pending_accept / adopted / rejected / expired |
| Version | 文档或事实的历史快照，用于回滚、审计与依赖追踪 | object_id、base_version、change_type、operator、timestamp | active / restored / superseded |

- 状态规则：
  - `confirmed` 表示该资产已被人工确认，可作为下游 AI 的常规事实来源
  - `locked` 表示该资产中的关键事实被升级为高优先级约束，AI 不得静默覆盖
  - `needs_review` 表示上游事实发生变化，下游文档需要人工复核，但系统不自动重写已有正文
- 关系规则：
  - `Project` 包含多个 `AssetDoc`、`OutputDoc`、`AIRun` 和 `Version`
  - `AssetDoc` 在被确认后可生成或更新多个 `LockedFact`
  - `AIRun` 必须绑定一个目标文档和一份上下文快照，`Suggestion` 必须从属于某次 `AIRun`
  - `Suggestion` 只有在被用户采纳后才能写入正文并形成新的 `Version`
  - `LockedFact` 更新后，所有依赖它的 `OutputDoc` 自动进入 `needs_review`

## 附录 C：内部工作说明 - AI 事实优先级与冲突处理详版
以下内容供内部方案与研发设计参考，用于把主文中的“AI 可控性原则”落到更细的机制层。

| 优先级 | 来源 | 是否可被低级来源覆盖 | 说明 |
| --- | --- | --- | --- |
| P1 | 系统硬约束（安全规则、导出格式规则、结构化 schema） | 否 | 平台级规则，所有工具统一遵守 |
| P2 | 用户当前显式操作（本次手动编辑、显式选中文本、明确确认的覆盖动作） | 否 | 代表用户此刻的最高意图 |
| P3 | `LockedFact` | 否，除非用户显式解锁或覆盖 | 是 AI 的首要事实来源 |
| P4 | 已 `confirmed` 的资产文档 | 原则上不可被未确认内容静默推翻 | 可作为常规事实来源 |
| P5 | 当前目标文档中用户手动保留的最新版本内容 | 可被用户本次显式改写覆盖 | 用于贴近当前写作语境 |
| P6 | 未确认资产、导入原文、参考资料 | 可以被更高优先级事实修正 | 作为候选背景，不作为硬约束 |
| P7 | 模板库、提示词库默认内容 | 可以 | 只提供写作框架，不定义事实 |
| P8 | 外部调研、开放资料、模型先验知识 | 可以 | 只能作参考，不得覆盖项目内高优先级事实 |

- 冲突处理规则：
  - 低优先级来源与高优先级来源冲突时，AI 结果只能进入 `Suggestion` 并带冲突标签，不得直接写回正文
  - 当用户明确选择“覆盖锁定事实”时，系统先创建新的 `Version` 和新的 `LockedFact`，再把依赖文档标记为 `needs_review`
  - AI 不得自动创建 `LockedFact`；只能提出“建议确认的事实项”，由用户人工确认后生效
  - 对涉及人物名、关系、时间线、关键设定、集数规格的冲突，必须给出具体冲突项，而不是泛化提示
